# Hints and "No More Winnable Moves" Warning (Lonelybot on-device)

Status: implemented + tested (2026-07-13); feedback rounds 1–2 done (2026-07-23): F1/F2/F7/F8 device-verified, F9 board-first hints implemented + Rust-tested + device-spot-checked (during F11 pass), F10 artifacts-in-git decision applied + proven, F11 classic stuck warning + three-settings split implemented + device-verified, F12 solver-line-quality analysis delivered (no work recommended). Feedback round 3 done (2026-07-24): F13 z-order fixes, F14 warning-mode select + sticky dead eras, F15 hint×warning decision point — all device-verified.

## User prompt

> Hey, a while back we talked about hints and kind of You know that in the settings we could have a new setting that's a hint that's off by default, but if it's on, like left to the undo button, there's a button called hint that would show you an next possible move And we also talked about the point that like it should tell you that There's no more possible moves, like a lot of the different solitaire apps have that, that they tell you there's no more like useful moves. Dad, you get this kind of warning? And the hint would kind of highlight the card that you could move, or like if you need to draw, it would highlight draw, just so you kind of when you're a bit tired you know what to like do next and if you kind of run, ran out of Yeah, ran, I ran out of patience or whatever. So the idea That's the idea, and at this point it was kind of hard to do, but at some point we also found a lonely bot solver with which we generate games on the Macbook, and there it was kind of part of the next step whether we could push that to, like, that Rust app if we could kind of bundle this in the app as well and use this kind of this, this engine for this as well. Please research, please do Prototype how this would work how this would like also figure out the performance a little bit, you know? What's, what's going on with this and how for these two features we could use that I guess this is quite a bit of, quite a bit of context please go ahead, I'm really looking forward to initial like findings and results and the prototype and the working version if possible.

## User prompt (feedback round 1, 2026-07-23)

> Commit the built artifacts. Please don't commit yet. I think there's still work to do about your lean stripping X86 from Sim Slice. I have no idea what that means. Please give me, like, in simple words, like the what why and what are the disadvantages and advantages and recommendation and why and so on and now and also, like, please do the proposed agents MD correction as always very concisely. And now, a couple of things. Things that I have observed. The hint button flickers on every move, which I guess it's like refreshing state and whatnot, but this is a no-go. Also, the hint goes through everything. to like, you know, I think the simple like the solver calls this save moves to do all the save moves, but this is kind of rough, to be honest, as you already mentioned. Are there options on the Rust Codex base to like optimize this or I mean an alternative, a pretty simple alternative would be to check if there's moves on the board or moves from anywhere without drawing and then seeing if it's still solvable next after doing that move and if yes you would give that as hint because like everything that's like nicely on the board would be the hint and that kind of gives it a bit of a nicer flow and if this is not possible then we like go for the draw I mean this would just be like one Entra step. I mean it wouldn't even be an Entra step if something on the board is possible and if not then it would be like Then it would kind of check if something afterwards is possible, and if not, then we do the draw there it would be an Entra step, but if nothing is possible then we don't do an Entra step. I don't know. And or kind of what other options are there to optimize this a little bit? And I know the author has said that he doesn't give like the guarantee for the fastest solve, which is a bit of an Entra duration. And I mean, I don't know. Please figure out and think through the different options here. Maybe you can also, even as a first step, present me the different options. And then where what feels like weird to me is like the message that nothing more is like no more good moves are possible. It messes with my gameplay, honestly. It's the gameplay is quite different because as soon as I see it I obviously go backend then I do something else and if I see it again I go backend like before I would go towards the end and then go back with the undo scrubber to where I think it was the mistake, and it was for me part of the charm to like You know You know find out where the mistake happened, and here it's like, you know, you just click yourself through it, and once you see it, you undo it and do something else, and you kind of see the critical path very easily from this. I'm not saying this is a bad thing per se, but it completely changes how you play the game. So I think at least here this should be like an additional setting. And I think it's a cool setting because, like, you can maybe experience the game differently, but it's different. And I think what people are used to is the kind of thing that you describe as heuristics. Maybe you can research that a bit closer online, which is you know, once there is really not no move left anymore, like really no like move that helps you towards doing something and I think this is actually pretty good or like research also if these heuristics are sometimes faulty because I've never experienced this as faulty I, think. And there is like really if there's nothing doable anymore, please also like mirror, describe exactly how this would work. And like, for example, usually it's even that nothing from the stock can be moved anymore, even though it's clear that this doesn't help you anything anymore. Like there, like the solidarabs go quite far, and they usually only calculate it once when you kind of restart the stock. And I do think that would be a cool option as well, probably a different option than the existing hint button. So I would probably ship this as like three different settings each described a little bit and Yeah, maybe it's also a bit of a combination if the if you click if you have the hint button on and the other things not and you click hint like you prop it will probably still show you, but it wouldn't show you proactively, or maybe it wouldn't, and yeah, but note then. I mean, I guess there's no hint available if if there is nothing. I mean, the hint could then also still be like, I mean, my thinking is that, you know, the hint shows you, like, what like the question is, does the hint show you Solving moves or any kind of moves because I guess existing solitaire apps show you any kind of moves, even if they're not guaranteed to kind of still uncover a card or even still win the game, which is quite different. I mean, please also mirror that back to me a little bit: that like one hint option would be like, hey, we're solving the game here, and the other hint option is just like, you know the thing hey I'm tired what's the next move is there still something something and kind of I think a lot of people are used to like you know being able to click themselves through until they get the heuristics, nothing works anymore, and then I don't know if they go back or give up or whatever. Probably a lot of people then give up. And this is just a completely different way of thinking, like the two different hints. One more thing: the hint only gives a blue outline for the draw, but not for moves on the stock. Or at least no, I mean it's super weird. If I move an ace up, it would like change the outline of the ace, which is like very off as like as a user, I think, hey, I need to move the ace, I didn't even see it for a long time, and this looks really bad. And also, I think for moves on the board. Like, I don't think they're shown properly. I think here like the experience definitely, definitely, definitely needs to be a bit polished. like if there's a move on the board, I don't even really see it. Like, there's nothing shown. I think this you can do directly. Thank you.

## User prompt (feedback round 2, 2026-07-23)

> Hey, no worries at all about the test game. this was like an actual you know generated test game from the demo modal window, so no issue whatsoever
> now the highlights show but please try some different things with like the visual way because like the blue is honestly qu,ite hard to see, even. I think we could do some more stuff here. And also, what confuses me is If there's the hint is to do a move to the foundation, it highlights both the foundation and the card. So, I like initially my reaction was that I moved something down from the foundation to the board. So, yeah, please improve just like this, showing, maybe even like do one option that you recommend and maybe think about some alternatives. Thanks for fixing the flicker. the sim slice thing. I mean, I guess we're regularly testing in the simulator, and the hints are a new thing. So like will the simulator still work without this? And if not, I don't think that's really an option. Question: Is should this even be committed? Like, you know, to me, this is like a, I don't know, like, for example, the all the build stuff from building Android stuff is also not committed, you know, all these like build files. And the question is a little bit: what is the source Codex Was the build stuff? Would it even make sense to git ignore all of this and like just have like a step where you can build it while you're like when you load it from Git. I'm not saying this is the right thing, please like search the web a little bit. What are best practices around this? How people usually do this, and honestly, just let's follow the best practice here. I let you decide this based on that best practice, especially also for Git. so about hint flow, first one seems doable. Second one, yes, makes sense, that's obviously off. I mean, my question was really now that we have like powerful AI these days, whether you know we optimize what LonelyBot SaaS but if you don't see a cheap way there, I mean, I guess, I mean, please do a quick analysis with a subagent whether that would be feasible reasonably. But meanwhile, just directly also implement one. for the heuristics part we could do the heuristics and when the heuristics SaaS no more useful moves. We then also run the solver to confirm this. The cool thing here would be like there's really like the Microsoft style, no more useful move. And please we also research this deeply. Like You know, their useful moves are also like moving stuff up and like moving it around, really, till everything is sorted, also, like moving stuff down from the stocktake it doesn't say there's no more useful moves, even if the solver couldn't finish it. But it really, like, there's no more useful moves, it's like you can't put stuff down anymore, you can't like from the stock, you can't put stuff to the foundation anymore, also not from stock to foundation, like this kind of heuristics. I think that's actually like you know, people also expect this like normal behavior, but you know, in addition to that, we could like have the Solver check that we that the heuristics is not wrong essentially. And I think the this we also could only show when we flip the like the stock back to the first card. I think this is how also like other games are typically doing this. I would do this as three different settings. The stocktaking like the really classic, no useful moves and by default on, the other two by default off.

## User prompt (feedback round 3, 2026-07-24)

> Hey, a couple of remarks. when I hit a hint the yellow thingy, honestly, I don't think it's super pretty, but that's like the least of my concerns right now. what i like is kind of there's an animation of of a card flight however what's super bad is that the card flight is behind all the other cards kind of you know if you move it to the foundation, it's behind the other foundation cards, even and if you move it to or from a board, it's behind the board with the other cards, it looks super bad. And also, the yellow ring is behind other cards, which, if there's space around, doesn't matter, for example, but in the like the three-card area in the stock, it's behind the second card, and also if the hint is if the hint is on the board like the yellow area is even behind the hidden cards or behind you know the cards in the in the same column which looks like it looks terrible Like at least for the card flights check out the absolute card layer refactor that we did that was a huge refactor and I think there we've done quite a bunch of stuff to fix that behavior for within the game. Maybe we could reuse this as well. about the no-winning moves left that you call nagging I kind of partly agree. I think once it's here we should just keep showing it until we're in a state where it's not true anymore. I think this is more of a kind of a permanent thing. We don't need to calculate it again every moment because, like, you know, once you've seen it once, it doesn't become untrue if you do further moves. Like, that's the whole concept of it. Like, once you're over there, you can only undo. what's also weird? If I click hint, It shows me a hint, but it shows me this warning at the same time, which doesn't like, you know, you're giving me a hint of what you can do, but you're also saying, hey, man, you cannot win this anymore. I think here we need to kind of keep the different modes and the combination of the modes like thought through. Like, if we do show this warning, then Like also let's not show a hint anymore and if we don't show the warning then we can kind of still do I feel like also the no more moves warning Like, I have not tried it yet. to the end, but like is this the same warning as the unwinnable warning? Then the unwinnable warning is kind of an extension of of the no more moves and maybe this is a little bit misleading that these are two separate settings that are independent of each other. Kind of maybe, maybe this would rather be like a select thing where we have no more moves warning or an unwinnable warning as soon as we detect that there's no more winnable path or as soon as like there's no more useful moves or it's completely off. yeah and And depending on the mode there, the hint button does something slightly different. Like if we're in no more moves warning like you know if there's if there's a warning if there's an outstanding warning then the hint button Should probably not show a hint anymore because well we told the user you can't actually do anything anymore. If we haven't reached that state yet of any of these three things, then the hint button should just show any like the hint as defined. yeah, but like right now, it shows a hint, even though the user is already informed that there are no more useful moves. Please also challenge this and let's do the next round. Looking forward, thank you

## Feedback round 3 (2026-07-24) — work items

- [x] F13. **Z-order fixes for hint visuals** — DONE (2026-07-24, code + full Android device pass on the A065; evidence `.test-artifacts/hints-f13/`). Root causes were Fabric view flattening + orderIndex z-sorting (NOT sibling order, NOT elevation) and the stock ring living in TopRow under the card plane — exact mechanics in Intermediary learnings. Fix: `HINT_OVERLAY_Z_INDEX = 20000` on the overlay root (same orderIndex mechanism the card flights' 10000+ boost uses, one band higher) + the stock ring moved into HintOverlayLayer (`resolveStockRect`, testID kept). Geometry resolvers untouched; visuals untouched (aesthetics deliberately deferred per user). Original item for reference: ghost glide + rings rendered BEHIND cards (foundation piles, board columns, waste-fan middle card, same-column cards incl. hidden ones).
- [x] F14. **Warning model: one select instead of two toggles** — DONE (2026-07-24: code, 33 suites/353 jest tests green, full Android device pass on the A065; evidence `.test-artifacts/hints-f14/` incl. EVIDENCE.md line-by-line log proof). Shipped: `hints.warningMode: 'off' | 'noUsefulMoves' | 'unwinnable'` (default `noUsefulMoves`) + `hints.hintButton`; migration chain explicit-warningMode → round-2 booleans (unwinnable=true → 'unwinnable'; else stuck=true → 'noUsefulMoves'; both false → 'off'; absent keys resolve to F11 defaults) → pre-F11 `hintsEnabled` (feeds the unwinnable input → lands 'unwinnable') → default, unit-tested as a matrix and verified live (the phone's round-2 payload landed on 'noUsefulMoves', a-01). UI: `WarningModePreference` picker row (labels Off / No more useful moves / Unwinnable game + one-line ladder description; Android stacks label ABOVE picker — see learnings). **Sticky dead era**: `deadEra {exactId, historyLength, mode}` set when a warning fires (either mode); inside the era (same deal, depth >= boundary) the warning is PERSISTENT (no timer) and ZERO evaluation runs (no heuristic, no solver — invariant commented on DeadEraMarker); exits on undo/scrub below boundary, new deal, hydrate, or mode change; era subsumed the F11 once-per-position dedupe refs AND the F11 stuck-vs-unwinnable precedence dance (one mode = one check). Persistent display: full GameNoticeBubble kept (judgment call, see learnings — never obstructed the board in any captured state). Deep links: `warnings:off|stuck|unwinnable` (+`nousefulmoves` spelling), round-2 keys as order-preserving semantic aliases via pure `resolveWarningLinkUpdate` (no silent downgrade of a stronger mode), `hints` alias kept; skill table updated.
- [x] F15. **Hint button × warning interplay** — DONE (2026-07-24, same gates + device pass as F14). ONE decision point in requestHint (decision table commented in code): (1) warning outstanding → NO move hint, NO solver call; re-affirm the warning (re-announce + bubble emphasis pulse via `warningEmphasisNonce`; stuck era re-affirms the stuck text, never "No hint found."). (2) no warning → solve: solved → winning hint; unsolvable → **fire-on-discovery**: if the active mode would warn at this position ('unwinnable' always; 'noUsefulMoves' at the stuck condition), the press's own proof starts the era right there instead of hinting — kills the debounce-gap contradiction (hint ring + warning moments later) completely; otherwise classic fallback (mode off / noUsefulMoves before the stuck state — no winnability leak beyond the user's chosen knowledge level); unknown → fallback only, never a warning on an unproven claim. Mode 'unwinnable' can only reach the fallback via 'unknown' (comment in code). Device-proven: press inside era = zero `[solver]` lines + no ring (b-04, c-04); mode-off lost game = classic fallback (d-01); solvable = winning rings (d-02).

## Summary

(update as we go)

- Research done: lonelybot core API mapped, bundling approach decided (local Expo module + C ABI, see `docs/external-package-guides/rust-mobile.md` refresh 2026-07-12).
- Desktop perf baseline: fresh-deal solves 0.03–0.45 ms on MacBook (incl. unsolvable proofs).
- Phase A done (2026-07-13): vendored lonelybot + `soli-solver-ffi` crate, JSON C ABI, 13 Rust tests green, artifacts built.
- Phase B done (2026-07-13): local Expo module `modules/soli-solver/` (Kotlin/JNI + Swift/@_silgen_name), `solverBridge.ts` + jest golden tests, `hintsEnabled` setting, `useHint` hook (button solve + debounced background unwinnable check), Hint button in the dock, stock pulse, `UnwinnableBubble`. All cheap gates green (typecheck/lint/jest 286, cargo test 13, clippy clean for our crate, JNI symbol verified in both `.so`s). Device builds (C2/C3) still open.
- Phase C largely done (2026-07-13): Android release build+install OK (solver `.so` in APK) but the phone dropped off wireless adb right after install — Android functional/perf pass still pending. iOS simulator: built after fixing the xcframework sim slice (x86_64 added) + one manual pod install; FULL functional pass green there (hint follow ~20 taps, draw-pulse incl. recycle, unwinnable bubble auto/button/undo-clear, hints-off silence, win silence). Sim perf: hint median 0.1 ms, background median 0.3 ms, max 3.5 ms over 55 solves.
- Feedback round 1, F1/F2/F7 done (2026-07-23): hint-button flicker fixed (solverBusy React state deleted — it toggled a disabled style around every sub-ms background solve), hint highlight rebuilt as a dedicated ring overlay decoupled from selection state (`HintOverlayLayer` + shared `HintRing`; SELECT_* pathway deleted), a11y announcements added. Cheap gates green (typecheck/lint/jest 30 suites/293 tests — jest needed the official react-native-worklets mock, see learnings). Full Android pass on the A065 phone: flicker proven fixed via frame-luma analysis, all hint ring classes screenshotted, clears verified, unwinnable bubble regression green, perf recorded (median 0.5 ms button / 0.9 ms background, max 2.5 ms over 31 solves). F3 (prefer-board hint) and F4 (setting split) remain pending user decisions.
- Feedback round 2, F8 done (2026-07-23): hint visuals redesigned — amber role-distinct rings (loud pulsing source + dark contrast contours; quiet static target) + one-shot ghost card gliding source→target as the direction cue (reuses CardVisual; gated on cardFlights, pulse on master; static rings when animations off). Gates green (30 suites/293 tests), full Android pass on the A065 incl. the ace→foundation case, ghost frame-step verification, repeat-press replay, flicker regression, animations-off branch. One on-device iteration (thicker source band, higher pulse floor, softer target). Evidence: `.test-artifacts/hints-f8/`.
- Feedback round 2, F9 done in Rust (2026-07-23, device pass pending): board-first hint selection inside `solve.rs` — after a solved main solve, no-draw progress candidates (tableau→foundation → waste-top→foundation → tableau run moves → waste-top→tableau, via non-dominance `gen_moves::<false>`) are each verified with a budget-sliced sub-solve; first verified candidate becomes the hint, else fall back to the main line's first move (typically draw). FFI contract unchanged, zero new vendor patches (`SolitaireEngine` applies candidates). Scan over 3041 winning-line positions: 1493 had draw-first raw lines, 580 (39%) now hint a board move. 6 new Rust tests (19 total green), incl. pinned fixtures for "board move beats draw line", "waste-top play beats draw line" and "the only board move provably loses → draw stays". Perf: mid-game P50 0.024 ms (baseline 0.022), P99 31 ms (19), zero budget hits.
- Feedback round 2, F10 done (2026-07-23): artifacts-in-git decision applied — artifacts + fingerprint gitignored, `scripts/ensure-rust-solver.js` (sha256 fingerprint over rust/ + build script; auto-rebuild or loud fail) wired into `yarn release`/`yarn ios`/`yarn build:ios`/`build-android.sh`, root `.easignore` mirror added. All four proofs done: rebuild-on-first-run (12 s warm, F9 inside), 33 ms silent fast path, touch→rebuild→revert→fingerprint byte-identical, git status artifact-free, `yarn release` path reaches its normal flow through the ensure step.
- Feedback round 3, F13 done (2026-07-24): hint-visual z-order fixed — root cause was Fabric flattening AbsoluteCardLayer's box-none wrapper and stable-sorting the hoisted card views by zIndex, so the un-zIndexed overlay lost to every card (both platforms, shared C++ core; elevation uninvolved). Overlay root now carries `HINT_OVERLAY_Z_INDEX = 20000` (above the flights' 10000+ band — the same mechanism, one band higher) and the stock ring moved from TopRow into the overlay (`resolveStockRect`; testID kept). Gates green (32 suites/324 tests); full Android overlap pass on the A065: waste-fan ring over neighbors, mid-column ring over face-down cards above, ghost over a 2-deep foundation at arrival, ghost over intermediate columns mid-flight, stock ring in both card-back and recycle states, clears + flicker regression. Evidence: `.test-artifacts/hints-f13/`.
- Feedback round 3, F14+F15 done (2026-07-24): warning model rebuilt as ONE select (`hints.warningMode` off/noUsefulMoves/unwinnable, default noUsefulMoves + `hintButton`; full migration chain from round-2 booleans AND pre-F11 `hintsEnabled`, matrix-tested + live-verified) with STICKY dead eras — a warning now persists (no timer) until the player exits the era (undo/scrub below the fire-depth boundary, new deal, hydrate, mode change) and, inside the era, zero heuristic/solver evaluation runs (monotone invariant commented on DeadEraMarker; the F11 dedupe refs + precedence logic collapsed into it). Hint press decision table (F15): outstanding warning → re-affirm (announce + bubble pulse, no solve); otherwise solve → winning hint / fire-on-discovery warning when the mode would warn / classic fallback (never a warning from 'unknown'). New `WarningModePreference` picker row (Android stacks label above picker — Compose row couldn't fit both), `warnings:` deep-link key + alias resolver, hook-level jest suite (react-test-renderer harness, solve-call spy). Gates 33 suites/353 tests; full Android pass on the A065 (a–f incl. zero-solver-call era proof, recycle persistence, undo-boundary clear + resume re-warn, mode-off classic behavior, obstruction judgment: full bubble kept). Evidence `.test-artifacts/hints-f14/`.
- Feedback round 2, F11 done (2026-07-23): three independent settings (`hints.stuckWarning` default ON / `hintButton` / `unwinnableWarning`, legacy `hintsEnabled:true` migrated to button+unwinnable once), classic Microsoft-style stuck warning (pure-TS `usefulMoves.ts` enumeration incl. draw-N reachable-stock simulation, solver-confirmed, shown only at the recycle-flip, era-deduped against the unwinnable bubble), Hint-button classic fallback on unsolvable/unknown (never leaks winnability), `UnwinnableBubble`→`GameNoticeBubble`, described settings rows (`DescribedSwitchRow`), `?set=` keys for all three (+`hints` alias). Gates green (32 suites/322 tests); full Android pass on the A065 incl. organic draw-5 stuck at the flip, mid-stock silence with zero solver calls, dedupe at a heuristic-dead flip, F9 board-first spot-check, 0 heuristic false positives across 21 live solves. Evidence: `.test-artifacts/hints-f11/`.

## Description

Two user-facing features, both backed by the Lonelybot Rust solver bundled into the app as a native module:

1. **Hint button**: a new setting "Hints" (off by default). When on, a Hint button appears to the left of the Undo button in the bottom dock. Tapping it highlights the next move on a winning path from the current position — the card to move (with its valid targets), or the stock if the right move is to draw.
2. **Unwinnable warning**: tell the player when the current position can no longer be won ("no more useful moves"). Stronger than other apps' heuristics because Lonelybot proves it exactly (we have perfect information: face-down identities are known in `GameState`).

Why Lonelybot: already validated offline for deal harvesting (`scripts/generate-solvable-deals-v2.js`), MIT-licensed, extremely fast, and its "thoughtful Klondike" model (all hidden cards known) matches the app's runtime knowledge exactly.

## Research results (2026-07-12/13)

### Desktop performance baseline (M-series MacBook)

`lonecli solve default <seed> <draw>`: 0.03–0.45 ms per solve across seeds 0–42, draws 1/3/5, including unsolvable proofs ("Impossible"). Prior harvest runs used a 5 s timeout as a safety bound; hardest known positions take minutes but are extremely rare. Mid-game positions are typically easier than fresh deals. Phones ~2–10x slower per core → expect single-digit ms typical; a budget handles outliers.

### Lonelybot core API (checkout `/Users/karim/kDrive/Code/Lonelybot/lonelybot`, upstream rev `ae30d39`, core lib locally unmodified, MIT)

- `#![no_std]` + alloc, single-threaded, `unsafe_code = "forbid"`, no global state. Local mods are lonecli-only.
- Card: `Card::new(rank 0..12, suit 0..3)`; internal suit order 0=♥ 1=♦ 2=♣ 3=♠ (HDCS). App order is clubs=0, diamonds=1, hearts=2, spades=3 (CDHS) → remap `[2,1,0,3]` (self-inverse), same as `harvest-v2`'s `app_card_index`.
- Solve: `solver::solve_with_tracking(&mut Solitaire, &impl SearchStatistics, &impl TerminateSignal) -> (SearchResult, Option<HistoryVec>)`; `SearchResult` = `Solved | Unsolvable | Terminated`. Search restores state after run. `TerminateSignal` trait → implement with `AtomicBool` + deadline (lonecli's local `TermSignal` does exactly this).
- Winning path: `HistoryVec = ArrayVec<Move, 1024>` of primitive moves: `DeckStack(Card) | PileStack(Card) | DeckPile(Card) | StackPile(Card) | Reveal(Card)`.
- Human-readable conversion: `convert::convert_moves(&mut StandardSolitaire, &[Move]) -> Result<ArrayVec<StandardMove, 1024>, InvalidMove>`; `StandardMove { from: Pos, to: Pos, card: Card }`, `Pos = Deck | Stack(u8) | Pile(u8)`; `DRAW_NEXT` sentinel = draw one step. A single primitive move can expand to several standard moves (n× draw + play).
- Draw step: `NonZeroU8`, 1–5 fine. Unlimited recycles (deck pointer wraps), matches app rules.
- Hidden cards: `Hidden` stores real identities per column; solver = thoughtful Klondike. Never call `hidden_clear`/`hidden_shuffle`.
- **Gap: no public mid-game constructor.** `Solitaire::new`/`StandardSolitaire::new` take a fresh 52-card deal only; all fields private; `Deck::set_offset`, `Hidden::from_piles`, `Stack` builders are `pub(crate)`. → We vendor the core crate and add a small, clearly-marked public `from_midgame` constructor (see steps). `From<&StandardSolitaire> for Solitaire` exists and handles conversion to the solver representation.
- Search is recursive DFS → run on a thread with 4 MiB stack (lonecli does the same; mobile default stacks are ~0.5–1 MiB).
- Transposition table is an unbounded `HashSet<u64>`; typical visits hundreds→tens of thousands. Budget bounds effective memory. `panic` risk low (asserts are `debug_assert!`); wrap FFI in `catch_unwind` anyway.

### Bundling decision (details + sources in `docs/external-package-guides/rust-mobile.md`, refresh 2026-07-12)

- **Local Expo module `modules/soli-solver/` + hand-written C ABI (JSON strings in/out), not uniffi-bindgen-react-native.** UBRN is still "not yet production" and polls Rust futures on the JS thread; overkill for a 1–3 function API.
- `modules/` is auto-discovered by Expo autolinking in SDK 57; no npm package needed.
- Expo `AsyncFunction` runs off the JS thread (Swift: serial `expo.modules.AsyncFunctionQueue`; Kotlin: dedicated HandlerThread). The Rust FFI additionally spawns its own 4 MiB-stack thread per solve and joins it (stack headroom + keeps the shared Expo queue responsive enough).
- Android: `cargo ndk -t arm64-v8a -t x86_64 -o <module>/android/src/main/jniLibs build --release`; NDK 27.1 (pinned by RN 0.86); cargo-ndk v4 auto-adds 16 KB page alignment (Play requirement for Android 15+).
- iOS: `aarch64-apple-ios` + `aarch64-apple-ios-sim` static libs, `IPHONEOS_DEPLOYMENT_TARGET=16.4`, packaged via `xcodebuild -create-xcframework`, vendored in the module podspec.
- Kotlin side loads via `System.loadLibrary` + `external fun`; Swift via bridging header in the pod.
- `catch_unwind` at every FFI entry, `panic = "unwind"`.

### Classic no-more-moves heuristic research (2026-07-23)

Commissioned in feedback round 1 (F4): how do existing apps implement the classic "no more useful moves" warning and hint buttons, how faulty are they, and what should Soli's classic (non-solver) warning check. Sources fetched 2026-07-23.

#### How mainstream apps behave

- **MobilityWare Solitaire** ([FAQ](https://mobilityware.helpshift.com/hc/en/10-solitaire/faq/86-what-does-no-useful-moves-mean/), updated ~2025): "No useful moves" = "no further moves for the current display of cards … other than drawing a new card or undoing". Per-position, event-driven, does NOT scan the remaining stock — a "draw or undo now" nudge that can appear with plenty of stock left. Random deals explicitly not guaranteed winnable.
- **Microsoft Solitaire** (Vista solitaire → Solitaire Collection → Solitaire & Casual Games): "No more moves!" prompt fires right after the triggering move, with end/undo options. Its move definition demonstrably excludes foundation→tableau digs and non-revealing rearrangements (see faultiness). Vista-era versions also sometimes stayed silent when stuck ([narkive thread, ~2008](https://microsoft.public.windows.vista.games.narkive.com/p7drlxQt/solitair-no-more-moves)).
- **Google Solitaire**: reportedly no stuck detection at all — only the win animation; player must notice and re-deal (third-party guides 2025/26 only; low confidence).
- **solitaired.com** ([daily page](https://solitaired.com/daily-solitaire), [app guide](https://solitaired.com/guides/app-guide)): "If no more moves are available, you lose the game"; deals carry offline-solver stats ("moves used by AI to win, if it's a solvable deal").
- **Solebon Klondike** ([site](https://www.solebon.com/klondike-solitaire---ios.html)): no documented no-moves warning; sells "Auto Hints keep the game moving", Winning Deals, "How to Win" replay.
- **Aisleriot** ([klondike.scm master](https://gitlab.gnome.org/GNOME/aisleriot/-/blob/master/games/klondike.scm), hint code ~1998–2008): deliberately NO game-over detection for Klondike. Source comment: the hints miss reversible moves ("unplopping" = foundation→tableau digs, shifts that enable digs) — "so we must NOT report game-over when they run out". Clearest implementation statement found.
- **PySolFC** ([hint.py](https://github.com/shlomif/PySolFC/blob/master/pysollib/hint.py) + `game/__init__.py getStuck()`, master 2026): optional "You are Stuck — There are no moves left…" dialog. Stuck = hint engine at a dedicated stuck level (reversible tableau moves filtered, no foundation digs, flips count) AND, if dealing is still possible, a layout snapshot — warn only when the same dead layout recurs after dealing through the talon. That is the "once per unproductive stock pass" compromise in code.
- **KPat (KDE)** ([apps.kde.org](https://apps.kde.org/kpat/)): the exception — a real solver in the statusbar ("winnable / not winnable in its current state / lost / timeout-unknown", [2009 dev thread](https://mail.kde.org/pipermail/kde-games-devel/2009-April/005455.html)). KDE3's heuristic predecessor was so conservative it almost never fired; the KDE4 solver verdict drew "omniscient guide ruins the game" backlash ([blog, 2009](https://talesfromthearmchair.net/productivity-gains-with-kde4.html)) — the same gameplay change Karim reported for our solver bubble. [TrySolitaire.com](https://trysolitaire.com/how-to-win-solitaire) (2026) likewise advertises a live solver winnable-checkmark.

Summary: commercial apps warn event-driven per move; only PySolFC implements per-stock-recycle timing (via snapshot-revisit, and only for the deal part). Move definitions vary but consistently EXCLUDE foundation→tableau digs and mostly exclude non-revealing rearrangements; MobilityWare doesn't even scan the stock.

#### Faultiness verdict

Documented wrong warnings (said stuck, wasn't):

- [lemoda.net, MS "Patience"](https://www.lemoda.net/windows/patience-bugs/index.html): finishable board; app declared "No more moves!" and even refused the winning jack→queen move; workaround was digging the 5♦ back off the foundation — the classic foundation→tableau miss.
- [Microsoft Q&A, Vegas Klondike](https://learn.microsoft.com/en-us/answers/questions/2677520/vegas-style-klondike-solitaire-in-solitaire-collec): "thinks that all moves have been made when there are still moves to make" — a 6-5-4-3 run had to move between two same-rank 7s (non-revealing rearrangement) to unbury a playable 7♠.
- [PySolFC #565](https://github.com/shlomif/PySolFC/issues/565) (2026-04): "'no more moves' messages when the game is clearly not stuck"; maintainer in [#574](https://github.com/shlomif/PySolFC/issues/574) (2026-06): "every time I fix the stuck check on one game, it seems to cause issues for another".
- [MobilityWare App Store review](https://appsupports.co/1556715867/solitaire-by-mobilityware/negative-reviews): winning deal + undo exploration → "the hints just say no useful moves detected".

False negatives too: Vista solitaire silent when stuck while "giving hints that obviously do not help" (narkive); PySolFC #574 (hints cycle pointless moves forever, stuck never fires). Verdict: shipped heuristics are wrong in both directions; the classic misses are exactly foundation→tableau digs, partial-run moves, and non-revealing-but-unblocking rearrangements. And no mainstream app claims "game unwinnable" from heuristics — every warning is framed as "stuck now: draw, undo, or re-deal"; only solver-backed products (KPat, TrySolitaire; offline winnable-deal databases) claim more. So: our solver bubble stays the strong statement; the classic warning must use the weaker "no useful moves" framing.

#### Hint buttons

Universal commercial pattern: heuristic "any productive legal move"; repeated taps cycle the list; no winnability guarantee — hints happily walk you into losing lines. [MobilityWare hint FAQ](https://mobilityware.helpshift.com/hc/en/10-solitaire/faq/1363-how-does-the-hint-button-work/): shows moves "deemed to possibly be productive", explicitly not all moves. Microsoft: Ctrl+H highlights one valid move at a time. Priority orders from open source:

- Aisleriot: ace→foundation → full-run tableau shifts → waste→tableau → other →foundation builds → text fallbacks ("consider an empty slot" → "deal a new card" → "try moving cards down from the foundation" → "no hint available right now").
- PySolFC `DefaultHint` scores: flip 100k > foundation drops ~90–93k (aces/2s first; deliberately lower when the card may still be needed for building) > tableau moves 80k + bonuses (empties a column +9k, uncovers a face-down +1.5k) > empty column 60k > waste plays ~30k > deal 0. Foundation→tableau only at debug level. Comment in code: "we do not cheat!" (never peeks at face-down identities).

No big app advertises live solver hints; closest are replays of pre-solved curated deals ("Show Me How To Win" / Solebon "How to Win") and solitaired's "AI best moves" marketing. When nothing is found: MobilityWare shows the no-useful-moves message, Aisleriot its text fallbacks, Microsoft the no-more-moves prompt. → Our solver hint is genuinely differentiated; a classic any-move hint would reuse the enumeration below, priority-ordered.

#### Recommended "useful move" enumeration for Soli (draw 1–5, unlimited recycles)

Blend of Aisleriot's exclusions + PySolFC's stuck filter, exact for our single ruleset; pure TS on `GameState`, never reads hidden identities:

```text
usefulMoveExists(state):
  if any tableau top or the waste top plays to a foundation            -> true
  if waste top fits a tableau top, or is a K and a column is empty     -> true
  for each column c with face-up run R (FULL run, from first face-up card):
    for each legal target of R:
      if c.hiddenCount > 0                                             -> true  # reveals
      if c.hiddenCount == 0 and R.base != K                            -> true  # frees a column
      # R.base == K with nothing beneath: can only go empty->empty, pure no-op -> skip (Aisleriot)
  for each column, each partial split of its face-up run:              # PySol step020; cheap, cuts false warnings
    if the split lands legally somewhere and the card it exposes plays to a foundation -> true
  # stock scan, draw-N reachability:
  simulate stock taps from (stock, waste) with NO plays, collecting every waste top,
    until a previously-seen (stock, waste) configuration repeats
    # terminates in <= ~2 full passes: the pre-recycle phase is transient; after one
    # recycle (waste flips back as a block, order restored) each pass is identical
  if any collected card plays to a foundation, fits a tableau top,
     or is a K while a column is empty                                 -> true  # hint: "draw"
  # excluded on purpose: foundation->tableau digs, non-revealing rearrangements —
  # matches every classic app; the solver-proven warning covers exactness
  return false
```

Draw-3/5 pitfall handled: reachability is NOT statically "every 3rd card" — playing a card out of the waste shifts the phase, changing which stock cards can surface. Re-running the whole check after every committed move (fresh simulation from the current stock/waste) keeps the phase correct; never cache the reachable set across moves.

#### When to trigger

After every committed move, debounced (reuse the 600 ms useHint debounce), pure TS on the JS thread — worst case a few hundred comparisons plus a ≤ ~50-tap stock simulation, microseconds. PySolFC checks the stock part only once per unproductive pass because its generic 500-game engine can't cheaply enumerate talon reachability per ruleset; with one known ruleset and a pure reducer we don't need that compromise — same "player has truly ground through everything" semantics, but immediate, without making the player physically cycle the deck. Clear the warning on undo/new position (same position-key mechanism as the solver bubble).

### Microsoft-style stuck warning — deep dive (2026-07-23)

Commissioned in feedback round 2 (F11): pin down Microsoft's exact "no more moves" semantics as the template for Soli's classic warning. Refines the Microsoft bullet in the section above. Sources fetched 2026-07-23. Confidence: [high] = primary evidence (screenshots/official text), [med] = triangulated player reports, [low] = inference.

#### Display form & wording [high]

- Windows 10/11 Microsoft Solitaire Collection, Klondike: MODAL dialog — title "**No more moves!**", body "You have run out of moves. Good game!", buttons **[OK]** (primary) + **[Undo last move]**. Read directly off the [lemoda.net screenshots](https://www.lemoda.net/windows/patience-bugs/index.html) (MSC UWP chrome with score/time bar; images downloaded + inspected 2026-07-23). No "keep playing" button — a Vegas-Klondike user on [Microsoft Q&A](https://learn.microsoft.com/en-us/answers/questions/2677520/vegas-style-klondike-solitaire-in-solitaire-collec) explicitly asks Microsoft to ADD a Continue button. OK accepts the loss; the only way to keep the deal alive is Undo last move [med — lemoda author continued only via undo + a different move].
- Lineage: Vista/Win7 wording "There are no more possible moves. What do you want to do?" with End Game / Return and try again (= undo) ([PC Review ~2007](https://www.pcreview.co.uk/threads/spider-solitaire-false-detects-no-more-possible-moves-situation.3428110/), [Win7 Spider Q&A 2011](https://learn.microsoft.com/en-us/answers/questions/2494970/how-to-disable-the-window-that-pops-up-in-spider-s)). Same modal end-or-undo pattern for ~15 years.
- No off switch: Win7-era players asked; unchecking "Show tips" does NOT disable it ([Microsoft Q&A](https://learn.microsoft.com/en-us/answers/questions/2451544/how-to-disable-the-prompt-there-are-no-more-moves)); none documented for MSC either. Microsoft ships it forced-on — and the complaints ("I want to figure it out for myself, it ruins the game") mirror Karim's round-1 objection verbatim [med].

#### What counts as a "move" — verdict on the generous-model hypothesis

- Counted: tableau→foundation, waste→tableau/foundation, revealing tableau→tableau moves, and — crucially — a legal draw/recycle [med-high, see timing].
- NOT counted, proven by documented false positives on winnable boards:
  - foundation→tableau digs — lemoda: dialog fired although digging 5♦ off the foundation made the board finishable (screenshots confirm diamonds foundation 5♦→4♦ after the workaround) [high];
  - non-revealing run rearrangements — Vegas Q&A: moving a 6-5-4-3 run from 7♠ onto an equal-rank 7♣ (unburies 7♠ for the foundation, flips no face-down card) was not seen; dialog fired right after the 7♣ came down [high for the case, med for the class boundary].
- So the user's mental model ("ANY placement counts; warns only when literally nothing can be placed") is half right: on placements Microsoft is LESS generous than that (it filters digs and rearrangements — exactly where its false alarms come from), on the stock it is MORE generous (an available draw counts as a move, see next).

#### When it is evaluated/shown

- Event-driven, immediate: checked after every committed move; the dialog appears right after the triggering move, not at a stock milestone (lemoda: fired one move after the prior screenshot, score 415→420; Vegas: "the last move was to bring down the 7♣"; Vista Spider: popup appeared mid-card-drag) [high]. NOT once-per-stock-pass.
- BUT in every documented Klondike firing the deck was already exhausted (lemoda: stock empty + the last waste card just played; Vegas: "all the cards are played"). No report found of it firing while a draw/recycle was still legal; Vista-era reports show the opposite failure — genuinely stuck, deck non-empty, silence plus useless hints ([narkive ~2008](https://microsoft.public.windows.vista.games.narkive.com/p7drlxQt/solitair-no-more-moves)). Best-evidenced model: **a legal draw/recycle suppresses the warning entirely — Microsoft never claims knowledge of unseen stock; the player must play the deck out (or use up Vegas passes) before it will declare no-more-moves** [med — fits all positive and negative evidence; partly an absence-of-reports argument].
- Draw-3 phase subtleties: undocumented; unknowable without instrumenting the app [low].
- Reset/re-warn: re-fires after EVERY move that re-enters a dead state (lemoda: "keeps on banging on and on" as he re-tried the jack move); any move/undo resets it. Effectively once per dead position, not once per game [high].

#### False-warning move classes (what Soli's solver gate rescues)

1. foundation→tableau dig wins the game (lemoda) [high]; 2. equal-rank run shift unburies a playable card (Vegas Q&A) [high]; 3. partial-run splits ([PySolFC #565](https://github.com/shlomif/PySolFC/issues/565) documents the class genre-wide) [med]. More modern-MSC false positives without board detail: [expert-Klondike daily challenge "tells me I am out of moves and I am not"](https://www.windowsphoneinfo.com/threads/microsoft-solitaire-collection-wont-open-daily-games.725229/), [FreeCell "ended with plenty of moves left"](https://learn.microsoft.com/en-us/answers/questions/2721669/solitaire-collections-games-ending-before-im-finis) [med]. Opposite failure exists too (Vista: stuck but silent) [med]. Soli mapping: heuristic-stuck AND solver-unsolvable → warn; heuristic-stuck but solver-solvable → suppress + log (rescues exactly classes 1–3); heuristic-not-stuck but solver-unsolvable → classic warning stays silent by design (that stronger claim belongs to the separate solver-warning setting).

#### Mainstream equivalents (genre expectation, one line each)

- MobilityWare: non-modal repeating toast "No useful moves" per display state — official FAQ: "no further moves for the current display of cards … other than drawing a new card or undoing"; fires with stock remaining, reappears after every fruitless draw incl. right after a recycle flip; never force-ends the game [high].
- Solitaired: loss framing — "if no more moves are available, you lose the game" [med].
- Zynga Klondike: no stuck warning documented; store copy sells "Use Shuffle when you are stuck" (power-up instead of warning) [med-low].
- Default-ON is the genre norm (Microsoft forced-on, MobilityWare on; no opt-outs documented anywhere) — supports classic warning default ON [med].

#### Recommended Soli spec (classic warning)

- Move definition: the `usefulMoveExists` enumeration above (foundation plays, waste plays, revealing/column-freeing run moves, foundation-enabling splits, plus exact reachable-stock simulation for draw 1–5 with unlimited recycles). Same exclusions as Microsoft (no foundation digs, no pure rearrangements) so the player-facing semantics feel identical — but our stock handling is strictly better: we prove "drawing won't help either" phase-correctly instead of requiring the player to grind the deck out.
- Gate: never display on the heuristic alone — require solver `unsolvable` on the same position key (existing 800 ms background path). Log suppressed cases (`heuristic stuck, solver solvable`) for QA; that log is the live counter of Microsoft-style false positives we prevented.
- Display timing — evidence vs user preference (user remembered "shows when you flip the stock back to the first card"; evidence says no mainstream app does that — Microsoft is event-driven-but-deck-exhausted, MobilityWare immediate/repeating; PySolFC's once-per-pass snapshot is an engine-cost compromise we don't share):
  - Option A (recommended): evaluate and show right after the triggering move (reuse 600 ms debounce), non-modal bubble. Matches the real genre norm, and since it only fires on solver-proven dead positions it cannot change strategy — undo or re-deal are the only continuations; earlier display just saves pointless deck-grinding.
  - Option B (user's remembered timing): identical evaluation, but hold the bubble until the player's next recycle tap (show immediately if stock+waste are already empty — Microsoft's actual firing case). Pro: the player has physically seen the cycle, so the warning reads as confirmation rather than oracle (no "how does it know cards I haven't seen?" moment). Con: lags the dead moment by up to a full pass, never shows if the player stops cycling, and matches no shipping app.
  - Recommendation: A. If playtest shows the mid-stock oracle-feel bothers, add B's display-hold as a refinement — the evaluation code is identical either way.
- Form/wording: non-modal bubble (existing `UnwinnableBubble` styling), no buttons, never blocks input — MobilityWare's form, not Microsoft's modal (the modal + missing Continue is the single most-complained-about part of Microsoft's design). Copy: "No more useful moves — undo or start a new game" (weak/classic framing per the faultiness verdict; reserve "unwinnable" wording for the solver setting).
- Reset semantics: mirror Microsoft — keyed to the existing position-key mechanism; any committed move/undo clears it; re-shows on each new dead position.

## FFI contract (single source of truth — Rust and TS must both follow this)

One exported function (plus a string-free): `soli_solver_solve(request_json) -> response_json` (caller frees via `soli_solver_free_string`).

Request JSON (app suit/rank conventions; card = `"<suit letter c|d|h|s><rank 1..13>"`, e.g. `"c1"` = A♣, `"s13"` = K♠):

```json
{
  "drawCount": 3,
  "budgetMs": 1500,
  "foundations": { "c": 2, "d": 0, "h": 1, "s": 0 },
  "tableau": [ { "hidden": ["d4", "s9"], "visible": ["h10", "s9"] } ],
  "stock": ["h2", "c7"],
  "waste": ["s5", "d12"]
}
```

- `foundations`: number of cards already on each foundation (2 = A,2 placed).
- `tableau`: exactly 7 entries; `hidden` and `visible` both ordered bottom→top (app column order). `visible[0]` sits on `hidden[last]`.
- `stock`: app order — **last element is drawn next** (app pops from end).
- `waste`: app order — **last element is the top/playable card**.
- Suit remap CDHS↔HDCS happens inside Rust (mirroring `harvest-v2`), so TS stays dumb.

Response JSON:

```json
{
  "status": "solved",
  "solveMs": 1.8,
  "visited": 3084,
  "winMovesRemaining": 42,
  "hint": { "kind": "move", "card": "d7", "from": { "type": "tableau", "index": 3 }, "to": { "type": "foundation" } }
}
```

- `status`: `solved | unsolvable | unknown | invalid | error` (`unknown` = budget hit; `invalid` = state failed validation; `error` = internal/panic).
- `hint` only when `solved`. `kind`: `draw` (tap stock once — even if several draws are needed before the play, one hint = one draw) or `move` with `from` ∈ tableau(index)/waste/foundation(suit), `to` ∈ tableau(index)/foundation. Derived from the first primitive move of the winning path via `convert_moves`; if the first standard move is `DRAW_NEXT`, the hint is `draw`.
- `winMovesRemaining`: primitive move count of the found line (for logs/QA).

## Acceptance Criteria

(Original Phase A–C criteria; F11 superseded the single-setting model — three toggles now, the button's unsolvable path shows a classic fallback hint instead of the unwinnable bubble, and a new default-ON stuck warning exists. Current behavior: see the F11 work item.)

- Settings has a "Hints" toggle in the Gameplay section, **off by default**.
- Setting off → UI identical to today. Setting on → Hint button left of Undo in the bottom dock.
- Tap Hint:
  - winning move exists → source card/pile highlighted (reuse selection + drop-hint visuals) or stock highlighted for a draw;
  - position proven unwinnable → non-blocking bubble "No winning moves left — undo or start a new game";
  - budget exceeded (rare) → bubble "No hint found".
- Unwinnable warning: with the setting on, after a move that makes the game unwinnable, the same bubble appears automatically (debounced, solver runs in background, cancelled/superseded by newer moves).
- Solver never blocks the JS thread; per-solve time budget enforced in Rust.
- `yarn typecheck && yarn lint && yarn jest` pass; Rust `cargo test` passes; Android on-device smoke test incl. measured solve times in logs.

## Open questions to the user (proceeding with recommendations)

1. **Scope of the automatic warning**: solver-proven "unwinnable" (recommended, implemented) vs. classic "no useful legal moves" heuristic. Lonelybot makes the exact check cheap; heuristics have false negatives/positives. → Proceeding with solver-proven.
2. **Warning gating**: warning only active when "Hints" setting is on (recommended v1: one setting, no background CPU for players who didn't opt in) vs. separate toggle. → Proceeding with single setting.
3. **Ship artifacts in git?** RESOLVED 2026-07-23 (F10): NOT committed — gitignored + fingerprint ensure step in every build path + `.easignore` for EAS uploads. See "Artifacts-in-git decision (2026-07-23)".
4. **Hint when status is `unknown`** (budget hit): v1 shows "No hint found". Could fall back to a TS "any legal move" heuristic later.

## Dependencies

- Vendored: lonelybot core crate (MIT, upstream rev `ae30d39`) → `rust/vendor/lonelybot/`, with `VENDORED.md` (rev, license, list of local patches). No new npm dependencies.
- Toolchain (dev machine only): rustup targets `aarch64-linux-android`, `x86_64-linux-android`, `aarch64-apple-ios`, `aarch64-apple-ios-sim`; `cargo-ndk`.
- Guides: `docs/external-package-guides/lonelybot.md`, `docs/external-package-guides/rust-mobile.md` (refresh 2026-07-12).

## UX/UI Considerations

- Hint button mirrors Undo's visual style (`UndoScrubber.tsx` dock); plain RN pressable (Tamagui/expo-ui buttons conflict with the pan gesture — see existing comment in `UndoScrubber.tsx`); lucide `Lightbulb` icon; a11y label "Hint" + testID `hint-button`.
- Highlight: dispatch the existing selection actions for the hint's source (`SELECT_TABLEAU`/`SELECT_WASTE`/`SELECT_FOUNDATION_TOP`) so the existing green drop-target borders show the destination automatically; auto-clear after ~2.5 s if untouched. `draw` hint: brief pulse/border on the stock pile (new, small).
- Unwinnable notice: non-blocking bubble modeled on `UndoHintBubble` (board-anchored, fade in/out, `pointerEvents="none"`, a11y announce). No modal.
- Don't run/show hints while `autoQueue` is active, while won, or in demo playback.

## Components

- Reuse: `UndoScrubber` dock, selection/drop-hint visuals, `UndoHintBubble` pattern, settings `FieldGroup` + `Switch`.
- New: `HintButton` (inside `UndoScrubber` dock row), `UnwinnableBubble` (clone of `UndoHintBubble` styling), stock-highlight prop on the stock pile rendering (`TopRow`/`AbsoluteCardLayer`).

## Steps to implement

Track status inline: [ ] todo, [x] done, [~] in progress.

### Phase A — Rust core + desktop prototype (validates the risky part first)

- [x] A1. `rust/` workspace in app repo: `rust/vendor/lonelybot/` (copy core crate from local checkout; exclude lonecli/python/benches/target), `VENDORED.md` with upstream rev + patch list. Workspace `rust/Cargo.toml` with `profile.release` lto+strip; `rust/target/` gitignored.
- [x] A2. Patch vendored crate: public `StandardSolitaire::from_midgame(hidden_piles, piles, deck_cards, draw_cur, stack, draw_step)` + `Deck::from_remaining` + `Stack::from_counts` + `Solitaire::is_valid` made pub + `Clone` on `StandardSolitaire`. All marked `// SOLI PATCH` and listed in `VENDORED.md`.
- [x] A3. `rust/soli-solver-ffi/` crate: JSON (serde) request/response per FFI contract; suit remap in Rust; state validation (52 cards exactly once, 7 columns, hidden≤i, legal visible runs, drawCount 1..=9); solve on spawned 4 MiB-stack thread with deadline `TerminateSignal`; `catch_unwind`; `extern "C"` `soli_solver_solve` + `soli_solver_free_string`; pure-Rust `solve_request_json` for tests/perf; `app_model::AppState` (test/perf-only mirror of app semantics).
- [x] A4. Rust tests (13 total, all green): fresh-deal equivalence seeds 0..20 × draw {1,3} (`equivalent_to` + identical solve result); draw-cycle equivalence incl. recycle wrap and consumed cards (validates `Deck::from_remaining` mask/map); replay-along-solution (seed 0 draw 1, 30 positions, status solved + hint legality vs app model); unsolvable fixture (scan seeds 0..50 × draws) → `unsolvable`; budget 0 ms → `unknown`; forced K♣ foundation hint (suit remap end-to-end); duplicate card / structural errors → `invalid`; raw FFI round-trip incl. null safety.
- [x] A5. Desktop perf harness `rust/soli-solver-ffi/examples/perf.rs`: draws {1,3,5} × 150 solvable seeds, winning-line walk re-solving every 5th position (every position for 30 seeds) through the JSON API, plus 50 known-unsolvable fresh deals per draw; P50/P90/P99/max + visited. Results below under ## Testing.
- [x] A6. `scripts/build-rust-solver.sh`: cargo-ndk build (arm64-v8a, x86_64, --platform 24) → `modules/soli-solver/android/src/main/jniLibs/`; iOS device+sim static libs (IPHONEOS_DEPLOYMENT_TARGET=16.4) → `SoliSolver.xcframework` → `modules/soli-solver/ios/`; header `rust/soli-solver-ffi/include/soli_solver.h`. Idempotent, prints artifact sizes. Run successfully (sizes below).

### Phase B — Expo module + app integration

- [x] B1. `modules/soli-solver/`: `expo-module.config.json`, podspec (vendored XCFramework), `build.gradle` (jniLibs), Swift + Kotlin `AsyncFunction("solvePosition", requestJson)` calling the C ABI (budget travels INSIDE the request JSON, not as a second arg); TS wrapper `modules/soli-solver/index.ts`. Includes the new Rust JNI export `src/android_jni.rs` (Kotlin can't call a C ABI directly); JNI symbol verified in both `.so`s via llvm-nm.
- [x] B2. `src/solitaire/solverBridge.ts`: `GameSnapshot` → request JSON + defensive response parsing; jest golden fixture from exactId `E1_0` (canonical deck — fully deterministic), mid-game reducer round-trip, card codec edges (6 tests).
- [x] B3. Setting `hintsEnabled` (default false) in `src/state/settings.tsx` + "Hints" Switch in `app/(tabs)/settings.tsx` Gameplay section.
- [x] B4. `useHint` hook: on-demand solve (budget 1500 ms) for the button; debounced (600 ms) background check per committed position (budget 800 ms, replace-latest queue, stale results ignored by position key, skip when autoQueue/won/demo/setting-off); `[solver] source=… status=… ms=… visited=…` console.log for device tests.
- [x] B5. UI: Hint button in `UndoScrubber` dock (absolute left-half pill mirroring Undo/DemoPlaylistHud, only when setting on); hint highlight via SELECT_* dispatch (auto-clear 2.5 s); stock pulse = outset green ring on the TopRow stock slot; `UnwinnableBubble` (clone of UndoHintBubble, no caret, carries both texts).
- [x] B6. Wired into `useKlondikeGame`/`KlondikeGameView` (props flow through existing undoScrubProps/topRowProps); dock also visible on fresh deals when hints on; demo playback excluded via `demoPlaybackActiveRef`; a11y label "Hint", testIDs `hint-button`/`hint-bubble`. Bonus for Phase C: `?set=` deep link learned `hints:on|off` (parser + launcher + SKILL.md table), so device tests toggle the setting without UI taps.

### Phase C — Testing

- [x] C1. Cheap gates (run by Phase B agent, 2026-07-13): `yarn typecheck` ✓, `yarn lint` ✓, `yarn jest` 29 suites/286 tests ✓; `cargo test -p soli-solver-ffi` 13 tests ✓; `cargo clippy --release` (host + `cargo ndk … clippy` for aarch64-android) — zero warnings in soli-solver-ffi (one pre-existing warning remains in vendored lonelybot, untouched); `./scripts/build-rust-solver.sh` ✓ + JNI symbol present in arm64-v8a and x86_64 `.so`s.
- [x] C2. Android release build on phone (`yarn release`) — DONE (2026-07-23, with the F1/F2 fixes in the build). History: the 2026-07-13 session built+installed fine (stale-Gradle-daemon fix, `libsoli_solver_ffi.so` verified in APK) but the phone dropped off wireless adb before any UI step; the functional pass ran on the iOS sim instead (C3). 2026-07-23: `ADB_WIFI_TARGET=192.168.1.12:5555 yarn release` (two devices connected — Mi MIX 3 also on adb; env var pins A065) built+installed+launched in 58 s. Full functional pass on the A065: hint button on fresh AND mid-game decks, 10 legal hint-follows (tableau single card, 3-card run, waste→tableau, 4 draw hints incl. recycle, A♣→empty foundation slot), every `[solver]` line `status=solved`, `moves` decremented exactly on plays and stayed constant across draws; unwinnable bubble on a draw-5 random re-deal (first re-deal already unsolvable, visited=10, 0.6 ms; bubble text verified on screenshot). Evidence: `.test-artifacts/hints-f1f2/` (22 screenshots + flicker videos). Not re-run this round: hints-off dock check (covered on iOS 07-13; F1/F2 don't touch the setting-off path — `hintButtonVisible` reduces to the identical pre-feature render).
- [x] C3. iOS simulator build (`yarn ios`) smoke test (2026-07-13, iPhone 17 Pro Max sim, Release): builds + links after two fixes (x86_64 sim slice in the xcframework — see Identified issues; manual `npx pod-install` needed once for the new local module) plus one machine-local node-path repair and one clean prebuild (`rm -rf ios`) to sync buildNumber 13→14. Smoke: `?set=hints:on` → Hint button on fresh deal; tap → `[solver] source=hint status=solved ms=0.1 visited=91 moves=84`, A♠ hint with green dashed drop-target border on the spades foundation (screenshot `.test-artifacts/hints-c2/ios-03-hint-highlight-fast.png`); background checks logged (`source=auto` 3.5 ms / 0.3 ms); `?set=hints:off` → Hint button gone, Undo-only dock after a draw, no solver logs. `@_silgen_name` linking worked as designed.
  - Extended iOS functional pass (ran the C2 scenario list on the sim while the Android phone was offline): ~20 hint taps followed end-to-end on a fresh draw-1 deal — foundation-move hints (A♣ col 4, A♠ col 7) highlighted source + green drop-target and were legal; long draw-hint stretch pulsed the stock ring every time (`ios-walk-04/07/…`), incl. the RECYCLE affordance on an empty stock (`ios-walk-15.png`) — one draw-hint = one stock tap incl. recycle, as designed; every solve stayed `status=solved`, `moves` decremented only on real plays (92→91→90) and stayed constant across draws (draws aren't primitive moves — expected). Unwinnable (draw 5 + solvableOnly off, 1st re-deal): auto bubble after a move (`ios-06-auto-bubble-after-move.png`), button re-show on demand (`ios-05-unwinnable-bubble.png`), bubble cleared on undo (`ios-08`) and re-warned once on the changed post-undo position key (documented as acceptable in useHint). Near-win fixture: waste-source hint (K♠) highlighted waste + foundation target (`ios-09-waste-hint-nearwin.png`); after winning, zero solver logs during celebration.
- [x] C4. Record on-device perf numbers in this doc — iOS simulator numbers (55 solves, 2026-07-13) and Android phone numbers (31 solves, 2026-07-23) recorded below.

## Plan: Files to modify

- New: `rust/**` (vendored crate + FFI crate + perf example), `scripts/build-rust-solver.sh`, `modules/soli-solver/**`, `src/solitaire/solverBridge.ts`, `src/features/klondike/hooks/useHint.ts`, `src/features/klondike/components/UnwinnableBubble.tsx`, tests.
- Edit: `src/state/settings.tsx`, `app/(tabs)/settings.tsx`, `src/features/klondike/components/UndoScrubber.tsx`, `src/features/klondike/components/KlondikeGameView.tsx`, `src/features/klondike/hooks/useKlondikeGame.ts`, stock pile rendering (`TopRow.tsx`/`AbsoluteCardLayer.tsx`), `.gitignore` (rust target dirs), `docs/external-package-guides/lonelybot.md` (embedding refresh note).

## Files actually modified

Phase A (2026-07-13):

- New `rust/Cargo.toml` — workspace (members: vendor/lonelybot, soli-solver-ffi); release profile lto + strip; `profile.dev.package.lonelybot opt-level=3` so tests don't crawl.
- New `rust/vendor/lonelybot/` — vendored core crate (src/ + LICENSE + standalone Cargo.toml + VENDORED.md with rev `ae30d391…` and SOLI PATCH list).
- SOLI PATCHes: `deck.rs` (`Deck::from_remaining`), `stack.rs` (`Stack::from_counts`), `standard.rs` (`StandardSolitaire::from_midgame`, `derive(Clone)`), `state.rs` (`is_valid` pub).
- New `rust/soli-solver-ffi/` — `src/protocol.rs` (contract types + card codec + suit remap), `src/solve.rs` (validation, mid-game build, threaded solve, hint derivation), `src/app_model.rs` (test/perf-only app-semantics state model), `src/lib.rs` (extern "C" boundary), `tests/solver_tests.rs`, `examples/perf.rs`, `include/soli_solver.h`.
- New `scripts/build-rust-solver.sh` (executable).
- Edit `.gitignore` — added `rust/target/`.

Phase B (2026-07-13):

- New `rust/soli-solver-ffi/src/android_jni.rs` — JNI export `Java_expo_modules_solisolver_SoliSolverJni_nativeSolve` wrapping `solve_request_json` (catch_unwind); edit `rust/soli-solver-ffi/Cargo.toml` (android-only dep `jni = "0.21"`, no default features), `src/lib.rs` (module decl), `src/protocol.rs` (pre-existing clippy `useless_format` fix).
- New `modules/soli-solver/` — `expo-module.config.json` (platforms apple+android), `package.json` (pins autolinking name `soli-solver`), `index.ts` (lazy `requireNativeModule` so imports never touch native), `android/build.gradle` + `android/src/main/AndroidManifest.xml` + `android/src/main/java/expo/modules/solisolver/SoliSolverModule.kt` (SoliSolverJni loadLibrary + `AsyncFunction Coroutine` on Dispatchers.Default), `ios/SoliSolver.podspec` (vendored_frameworks) + `ios/SoliSolverModule.swift` (@_silgen_name C decls, plain-closure AsyncFunction). Built artifacts (jniLibs `.so`s + `SoliSolver.xcframework`) land here via the build script.
- New `src/solitaire/solverBridge.ts` — request/response types mirroring protocol.rs, `buildSolverRequest`, `parseSolverResponse`, `cardToSolverCode`.
- New `test/unit/solitaire/solverBridge.test.ts` — golden fixture (exactId `E1_0`), mid-game round-trip, codec edges, response parsing.
- New `src/features/klondike/hooks/useHint.ts` — both hint features; single replace-latest solve queue; position-keyed bubble/pulse.
- New `src/features/klondike/components/UnwinnableBubble.tsx`.
- Edit `src/state/settings.tsx` (+`hintsEnabled` default false, setter, merge), `app/(tabs)/settings.tsx` ("Hints" switch), `src/features/klondike/components/UndoScrubber.tsx` (hint props, Hint pressable, UnwinnableBubble), `src/features/klondike/components/cards/TopRow.tsx` (`stockPulse` ring), `src/features/klondike/hooks/useKlondikeGame.ts` (useHint wiring, dock visibility, prop threading), `src/features/klondike/hooks/useDemoGameLauncher.ts` + `test/unit/features/klondike/demoLinkParsing.test.ts` + `.agents/skills/soli-testing/SKILL.md` (`?set=hints:on|off` deep link for device tests), `docs/external-package-guides/rust-mobile.md` (implementation-notes addendum 2026-07-13).

Phase C (2026-07-13):

- Edit `scripts/build-rust-solver.sh` — iOS simulator xcframework slice now universal (adds `x86_64-apple-ios` build + lipo merge with inline comment on WHY; fixes the sim link failure below). Artifacts rebuilt in place (`modules/soli-solver/**`).
- Edit `.agents/skills/soli-testing/SKILL.md` — clean-iOS-build guidance now names new local Expo modules explicitly; two troubleshooting rows added (stale Gradle daemon node error; `.xcode.env.local` dead brew path).
- Machine-local, not in git: `ios/.xcode.env.local` pinned to `/opt/homebrew/bin/node` (gitignored; `ios/` is fully gitignored and was regenerated once via clean prebuild).
- Test evidence: `.test-artifacts/hints-c2/` (28 screenshots + iOS log stream capture).

Feedback round 1 — F1/F2 (2026-07-23):

- Edit `src/features/klondike/hooks/useHint.ts` — F1: `solverBusy` state deleted (refs only); F2: dispatchless — SELECT_*/CLEAR_SELECTION pathway, `hintToSelection`, `selectionsEqual` removed; draw pulse + move highlight unified into one position-keyed `highlight` state (2.5 s timer, restart on repeat, effect-clear on position change); a11y announcement `getHintAnnouncement` (exported for tests).
- New `src/features/klondike/components/cards/HintOverlayLayer.tsx` — shared `HintRing` visual (3 px green `COLOR_DROP_BORDER` + glow, self-outsetting), pure `resolveHintRects` (exported for tests), memoized overlay component (mounted only while a move hint shows).
- Edit `src/features/klondike/components/cards/utils.ts` — `AbsoluteCardLayerLayouts` + `createEmptyAbsoluteCardLayerLayouts` + `resolveTopRowPosition` + `resolveTableauPosition` moved here from AbsoluteCardLayer.tsx (pure geometry, jest-importable without the component's tamagui imports); AbsoluteCardLayer re-exports the type+creator for existing consumers.
- Edit `src/features/klondike/components/cards/AbsoluteCardLayer.tsx` — imports the moved geometry from utils; no behavior change.
- Edit `src/features/klondike/components/cards/TopRow.tsx` — stock pulse ring now renders the shared `HintRing` (testID `hint-stock-ring`).
- Edit `src/features/klondike/components/UndoScrubber.tsx` — `hintDisabled` prop + disabled style removed; comment documents the no-busy-styling decision.
- Edit `src/features/klondike/hooks/useKlondikeGame.ts` — useHint call without dispatcher, `hintOverlayProps` (null during normal play) added to viewProps, `hintDisabled` wiring removed.
- Edit `src/features/klondike/components/KlondikeGameView.tsx` — renders `HintOverlayLayer` between the card layer and the celebration overlay.
- Edit `src/solitaire/solverBridge.ts` — `parseSolverCardCode` (reverse codec) + `SolverMoveHint` type.
- New `test/unit/features/klondike/hintOverlay.test.ts` — 6 tests: run-spanning source rect, empty-column target, waste-fan top card, foundation source/target slots, null on unmeasured layouts / face-down hint card, announcement strings.
- Edit `package.json` (jest config) + new `test/setup/mock-worklets.ts` — official `react-native-worklets` jest mock + transformIgnorePatterns entry (see learnings).

Feedback round 2 — F8 (2026-07-23):

- Edit `src/features/klondike/constants.ts` — `COLOR_HINT = '#FFB020'` added to the palette block (comment documents why not green/pure yellow/tamagui token).
- Edit `src/features/klondike/components/cards/utils.ts` — `resolveHintRects`, `HintRect`, helpers moved in from HintOverlayLayer (kept 1:1; jest-importable home now that the overlay pulls tamagui).
- Rewrite `src/features/klondike/components/cards/HintOverlayLayer.tsx` — role-distinct `HintRing` (source: amber+glow+dark contours+reanimated pulse; target: translucent static), new `HintGhost` (CardVisual copy gliding source→target one-shot, replay on repeat press), gating via useAnimationToggles (pulse: master; ghost: cardFlights).
- Edit `src/features/klondike/components/cards/TopRow.tsx` — comments only (stock ring inherits role='source' default; no prop changes).
- Edit `test/unit/features/klondike/hintOverlay.test.ts` — resolver imports point at cards/utils; all 6 tests unchanged otherwise.

Feedback round 2 — F9 + F10 (2026-07-23):

- Edit `rust/soli-solver-ffi/src/solve.rs` — F9: `board_first_candidates` + `pick_board_first_move` (non-dominance gen, priority order, budget-sliced verification sub-solves on the solve thread), hint derivation prefers the verified candidate (`derive_hint(std_game.clone(), &[m])`) with main-line fallback; module doc + inline decision comments (visited/winMovesRemaining semantics, slice telescoping, engine choice).
- Edit `rust/soli-solver-ffi/tests/solver_tests.rs` — 5 new tests + helpers (`std_game_from_request` mirrors build_state via public APIs, `raw_first_standard_move`, `no_draw_candidates`, `hint_to_standard_move`); pinned fixtures `BOARD_FIRST_FIXTURE` / `DRAW_ONLY_FIXTURE` as full request JSON with provenance comments.
- Edit `rust/soli-solver-ffi/examples/perf.rs` — cargo fmt only (two pre-existing formatting drifts; no behavior change).
- New `scripts/ensure-rust-solver.js` — F10 fingerprint ensure (see F10 item for design).
- Edit `scripts/build-install-android.js`, `scripts/build-install-ios.js` — `ensureRustSolverArtifacts()` after the build lock, with placement-rationale comments.
- Edit `package.json` — `build:ios` prepends `node scripts/ensure-rust-solver.js &&`.
- Edit `build-android.sh` — ensure call before the Gradle bundle (Play Store path).
- Edit `.gitignore` — solver artifact block (jniLibs/, SoliSolver.xcframework/, .artifacts-fingerprint.json) + easignore-sync note.
- New `.easignore` — .gitignore mirror minus artifact lines, semantics documented in header.
- Rebuilt (not committed, now gitignored): `modules/soli-solver/android/src/main/jniLibs/*.so`, `modules/soli-solver/ios/SoliSolver.xcframework`, new `modules/soli-solver/.artifacts-fingerprint.json`.

Feedback round 2 — F11 (2026-07-23):

- New `src/solitaire/usefulMoves.ts` — classic useful-move enumeration (`findFirstUsefulMove` returning a `SolverHint`, `hasUsefulMove`, `collectReachableWasteTops`), full research provenance + exclusions in the module doc.
- Edit `src/solitaire/klondike.ts` — exported `canDropOnTableau`, `canDropOnFoundation`, `KING_RANK` for usefulMoves (comments note why: one legality source).
- Edit `src/state/settings.tsx` — `hintsEnabled` replaced by `hints: { stuckWarning, hintButton, unwinnableWarning }` (+`HintPreferenceKey`), defaults true/false/false, `setHintPreference`, `hintPreferenceDescriptors` (label+description copy), legacy-key migration inside `mergeSettings`; `DEFAULT_SETTINGS`+`mergeSettings` exported for tests.
- New `components/settings/DescribedSwitchRow.tsx` — label+description+switch row from @expo/ui primitives (see learnings for why not ListItem).
- Edit `app/(tabs)/settings.tsx` — three described rows in Gameplay via the descriptors.
- Rewrite `src/features/klondike/hooks/useHint.ts` — three flags replace `enabled`; ONE merged background effect (unwinnable precedence → stuck confirm), stock-empty-gated stuck heuristic, `[stuck-heuristic]` telemetry, `unwinnableEraRef` dedupe, button fallback on unsolvable/unknown, `STUCK_BUBBLE_TEXT` + `getHintFallbackNoticeText` exports.
- New `src/features/klondike/components/GameNoticeBubble.tsx` (renamed from `UnwinnableBubble.tsx`, deleted; testID `hint-bubble` kept); edit `UndoScrubber.tsx` (import + prop comments).
- Edit `src/features/klondike/hooks/useKlondikeGame.ts` — three settings destructured, useHint wiring, dock visibility on `hintButton` only, `setHintPreference` forwarded to the demo launcher.
- Edit `src/features/klondike/hooks/useDemoGameLauncher.ts` — `?set=` parser: `HINT_LINK_KEYS` map (stuckwarning/hintbutton/hints-alias/unwinnablewarning), `SettingsLinkUpdates.hintPreferences`, launcher applies via `setHintPreference`.
- Tests: new `test/unit/solitaire/usefulMoves.test.ts` (18 tests: every enumeration rule, exclusions incl. digs/king-shuffles, draw-count-awareness, draw-3 phase-shift pair, reachability pass math), new `test/unit/state/settingsHints.test.ts` (7 tests: defaults, migration, explicit-wins, junk tolerance), rewritten `test/unit/features/klondike/demoLinkParsing.test.ts` (new keys + alias), extended `hintOverlay.test.ts` (notice-text variants).
- Edit `.agents/skills/soli-testing/SKILL.md` — `?set=` row: new keys + alias.

Feedback round 3 — F13 (2026-07-24):

- Edit `src/features/klondike/components/cards/HintOverlayLayer.tsx` — `HINT_OVERLAY_Z_INDEX = 20000` on the overlay root (above the card layer's 10000+ flight band; the long comment documents the Fabric flattening/orderIndex root cause); props take `SolverHint` (both kinds) and the layer renders the stock draw ring (`hint-stock-ring`, role source) for `kind: 'draw'` via the new `resolveStockRect`.
- Edit `src/features/klondike/components/cards/utils.ts` — new `resolveStockRect(layouts, metrics)` (stock-slot rect from the same layout registry; pure/jest-importable).
- Edit `src/features/klondike/components/cards/TopRow.tsx` — stock ring + `stockPulse` prop removed (comment left in place warning not to move it back under the card plane); HintRing import dropped.
- Edit `src/features/klondike/hooks/useHint.ts` — returns one `activeHint: SolverHint | null` instead of `stockPulse` + `hintHighlight` (the overlay owns all hint kinds now).
- Edit `src/features/klondike/hooks/useKlondikeGame.ts` — wiring for `activeHint`; `stockPulse` removed from topRowProps; `hintOverlayProps` mounts for ANY active hint.
- Edit `src/features/klondike/components/KlondikeGameView.tsx` — corrected the (wrong) "later sibling paints above the cards" comment.
- Edit `src/features/klondike/components/cards/AbsoluteCardLayer.tsx` — comment cross-reference at the flight zIndex boost (keep it below HINT_OVERLAY_Z_INDEX).
- Edit `test/unit/features/klondike/hintOverlay.test.ts` — 2 new `resolveStockRect` tests (rect from registry; null when unmeasured).

Feedback round 3 — F14/F15 (2026-07-24):

- Edit `src/state/settings.tsx` — `WARNING_MODES`/`WarningMode` union; `hints` becomes `{warningMode, hintButton}`; `parseWarningMode` + the four-step migration chain inside `mergeSettings` (explicit → round-2 booleans → hintsEnabled → current, fully commented); `setHintPreference` replaced by `setWarningMode` (accepts functional updates — deep-link aliases resolve against the current mode) + `setHintButtonEnabled`; descriptors replaced by `hintButtonPreference` + `warningModePreference` (labels/description/options copy).
- New `components/settings/WarningModePreference.tsx` — picker row + ladder description; platform-split layout (iOS label-left row, Android label above picker) with the device-evidence comment.
- Edit `app/(tabs)/settings.tsx` — hint-button DescribedSwitchRow + WarningModePreference replace the three-toggle map.
- Rewrite (large) `src/features/klondike/hooks/useHint.ts` — `DeadEraMarker {exactId, historyLength, mode}` + invariant doc; deadEra state+ref pair (`setDeadEra` keeps them in lockstep); `fireWarning` (persistent, no timer, announce + emphasis nonce); era-exit effect (declared before the evaluation effect — effect order is load-bearing) + mode-change clear effect; background effect gates on the era at schedule time, fire time AND response time (in-flight race with fire-on-discovery); F15 decision table in `requestHint` (re-affirm / fire-on-discovery / `showClassicFallback`); F11's `lastAutoUnwinnableKeyRef`/`lastStuckKeyRef`/`anyFeatureEnabled` deleted; outputs `warningText ?? noticeText` + `warningEmphasisNonce`.
- Edit `src/features/klondike/components/GameNoticeBubble.tsx` — optional `emphasisNonce` prop replays a scale pulse (two multiplying scale transforms: entry × emphasis).
- Edit `src/features/klondike/components/UndoScrubber.tsx` — `warningEmphasisNonce` prop threaded to the bubble.
- Edit `src/features/klondike/hooks/useKlondikeGame.ts` — warningMode/hintButton destructure, useHint + launcher wiring, nonce into undoScrubProps.
- Edit `src/features/klondike/hooks/useDemoGameLauncher.ts` — parser: `warnings:` key (off|stuck|nousefulmoves|unwinnable), round-2 alias keys → ordered `WarningLinkUpdate[]`, `hintButton`/`hints` boolean; pure `resolveWarningLinkUpdate` (exported); launcher applies via functional `setWarningMode`.
- Tests: rewritten `test/unit/state/settingsHints.test.ts` (migration matrix incl. both legacy generations, junk tolerance), rewritten `test/unit/features/klondike/demoLinkParsing.test.ts` (warnings key + alias truth table + F11-restore-sequence regression), new `test/unit/features/klondike/hintWarnings.test.tsx` (15 hook-level tests: era persistence with solve-call spy, boundary semantics, mode change, full F15 decision table).
- Edit `.agents/skills/soli-testing/SKILL.md` — `?set=` row: warnings select + alias semantics.

## Intermediary learnings

- `lonecli solve` prints "Run in X ms" = actual solve time; sub-ms on desktop for typical deals, `Impossible` for unsolvable, `Terminated` on budget.
- Suit remap between app (CDHS) and lonelybot (HDCS) is `[2,1,0,3]` and self-inverse; keep it in Rust only.
- Lonelybot `Deck` layout for mid-game: `[waste bottom→top, stock next-draw→bottom]`, `draw_cur = waste.len()`. App stock pops from array end → reverse app stock when serializing. Verify via fresh-deal equivalence test rather than trusting docs.
- Expo AsyncFunction thread stacks are too small for lonelybot's recursive DFS worst cases → FFI spawns its own 4 MiB thread per solve (lonecli does the same).
- **Hidden ordering verified empirically (A4)**: `HiddenVec` is bottom→top — the LAST element is revealed next (`StandardSolitaire::do_move` reveals via `.pop()`). Matches the request contract ("hidden bottom→top") 1:1, no reversal needed. An earlier note claiming "first element = topmost" was wrong.
- `Deck::from_remaining` can declare the current physical order canonical (fresh dense `map`/`mask` over len cards): `find_card`/`encode`/`decode` only depend on relative order, which play preserves. Proven by the draw-cycle equivalence test incl. recycle wrap and consumed cards.
- Lonelybot's `Solitaire` cannot represent "hidden cards but no visible card" in a column (the base of every face-up run is tracked inside `Hidden`); the app auto-flips so this never occurs — request validation rejects it.
- Visible runs must be validated (descending, alternating colors, arbitrary base): `Solitaire`'s compact K+ representation reconstructs runs from a bitmask and would silently solve a *different* position for an illegal run.
- A tap on an empty stock is recycle-only in both the app and lonelybot (`deal_once` at end wraps without drawing) — one `DRAW_NEXT` = one stock tap, so the "draw" hint covers recycling too.
- `convert_moves` needs a `StandardSolitaire` clone (consumes the state by replaying) → SOLI PATCH derives `Clone`.
- lonecli's `<draw>` CLI arg maps directly to `NonZeroU8` draw_step (no off-by-one).
- Known-unsolvable fresh deal for tests: default seed 4, draw 5 (found by scan; test scans 0..50 so it stays robust).
- The whole Rust workspace lives in the kDrive-synced folder; cargo cold builds there are extremely slow when the machine is under heavy load (43 min for the first debug build during a load-average-400 spike; later warm builds minutes). If this stays painful, consider `CARGO_TARGET_DIR` outside kDrive.

Phase B (2026-07-13):

- **Kotlin↔Rust JNI naming**: the exported symbol must be `Java_<package with _>_<class>_<method>` — `Java_expo_modules_solisolver_SoliSolverJni_nativeSolve` for `object SoliSolverJni` in `expo.modules.solisolver`. Use `extern "system"` (JNI calling convention), jni crate 0.21.1 as android-only dep. Verify with `$NDK/toolchains/llvm/prebuilt/darwin-x86_64/bin/llvm-nm -D --defined-only <so> | grep Java_` (plain `nm -gU` is Mach-O-only).
- **Swift links the Rust C symbols via `@_silgen_name`**, not the bundled header: a Swift-only pod has no bridging header, and exposing a vendored xcframework's headers through CocoaPods umbrella/module-map machinery is the fragile path. `@_silgen_name` declares the two symbols directly; the linker resolves them from the vendored static lib. Trade-off: no compiler check against `soli_solver.h` — signatures are mirrored manually (2 functions). If Phase C linking ever fails, fallback: copy the header next to the podspec + `s.public_header_files`.
- **Local-module autolinking**: `expo-modules-autolinking` (dependencies/scanning.js) gives search-path modules without package.json a MOCK name (`local-module`) — a minimal `package.json` with `name: "soli-solver"` keeps resolution deterministic. Local modules are imported by relative path (`modules/soli-solver`), not by package name.
- Kotlin `AsyncFunction("x") Coroutine { … }` needs `import expo.modules.kotlin.functions.Coroutine` (infix fun on AsyncFunctionBuilder).
- **Solves cannot be cancelled in v1** (no cancel hook wired through the FFI): `useHint` uses a single replace-latest queue (never two native calls in flight) and drops results whose position key no longer matches. Budget inside Rust caps the damage of an abandoned solve at ~1.5 s of background CPU.
- **Position identity key** = `exactId|moveCount|history.length|stock.length|waste.length`: moveCount covers plays/draws, history.length covers undo/scrub (which keep moveCount), exactId covers new deals. Bubble + stock pulse are keyed to it, so any board change hides them without cleanup effects.
- **Foundation counts imply cards**: a request's 52-card completeness check must expand `foundations` counts into A..n of each suit (the jest round-trip test initially forgot this and counted 51).
- exactId `E1_0` (permutation rank 0) decodes to the canonical deck — ideal deterministic golden fixture, no RNG.
- **Dock visibility**: the undo dock hides on an empty timeline, which would make a fresh-deal hint unreachable — with hints ON the dock now also shows on empty timelines (Undo pill inert+dimmed via existing `canUndo`); with hints OFF the expression reduces to the pre-feature `shouldShowUndo` exactly.
- SELECT_* on an identical selection TOGGLES it off in the reducer — `useHint` skips the dispatch when the hint selection is already active (rapid double hint-press would otherwise un-highlight).
- The undo pan gesture's `hitSlop` extends 20 px left of the Undo pill, overlapping the Hint button's right edge by ~12 px: plain taps there still hit the Hint pressable (pan needs 5 px horizontal movement); a horizontal drag from that sliver may start a scrub — accepted.
- `yarn format:check` flags several files not touched by this feature (pre-existing); only the feature files were formatted.

Feedback round 1 (2026-07-23):

- **Per-solve React state is a UI hazard**: any `setState(true/false)` bracketing a sub-ms async operation renders a 1–2 frame style blink wherever that state styles something (here: `solverBusy` → `hintDisabled` → button opacity on every move). If the busy signal has no >1-frame consumer, keep it in refs.
- **In production, ONLY useHint ever dispatched SELECT_*/CLEAR_SELECTION** — regular play is tap-to-auto-move (`APPLY_MOVE` directly). After decoupling hints from selection, `state.selected` stays null in normal play; the engine's selection actions/visuals remain as reducer/test surface (not removed — engine untouched on purpose).
- **jest + reanimated 4**: the first unit test to (transitively) import `react-native-reanimated` (via klondike `constants.ts` → `Easing`) crashed jest: `react-native-worklets` instantiates its native TurboModule proxy at module scope. Official fix applied: allow-list `react-native-worklets` in `transformIgnorePatterns` AND `jest.mock('react-native-worklets', () => require('react-native-worklets/src/mock'))` in a `setupFiles` entry (`test/setup/mock-worklets.ts`). Note: reanimated's own `react-native-reanimated/mock` does NOT work as a moduleNameMapper target — it re-imports the real `./index` for some re-exports and crashes the same way.
- **Keep pure geometry out of component modules**: importing `AbsoluteCardLayer.tsx` in jest drags in CardVisual → tamagui (ESM, untransformed). Moving the layouts type + slot resolvers to `cards/utils.ts` made `HintOverlayLayer`'s resolver (and its tests) importable without widening jest transforms for tamagui.
- Tableau source rings enclose the whole moved run, not just the hint card: cards stacked on the run base move with it, and a base-card-sized ring would have those cards crossing its bottom border.
- **Flicker verification technique** (cheap, objective): `adb shell screenrecord` (VFR — frames are only encoded when pixels change, so a 1-frame blink WOULD be captured), then `ffmpeg -vf "crop=<button rect>,signalstats" -f framemd5/-metadata YAVG` per frame. Raw frame MD5s differ from H.264 noise; per-frame average luma (YAVG) is the right metric — a stable button stays within ~0.15/255.
- Two devices on adb: `yarn release` needs `ADB_WIFI_TARGET=<ip:5555>`, everything else (`android-ready.sh`, raw adb) needs `ANDROID_SERIAL`; `yarn deeplink` takes `--serial`. agent-device `open --device "A065"` worked despite the phone appearing twice (wifi + tls mdns).
- This agent-device version (0.19.3) has no `find <needle> press` action — press by `id="…"`/`label="…"` selectors; testIDs DO surface as Android resource-ids here (press `id="stock"`, `id="hint-button"` worked; the accessibility.ts header's caution about testID→resource-id inconsistency is at least device-dependent).
- agent-device `screenshot` adds ~2 s latency and can miss the 2.5 s ring window after a press round-trip; `adb exec-out screencap -p` right after the press is fast enough. One hint screenshot (05) missed the rings purely due to this.
- **Hint-follow + Auto Up = the game can finish itself**: following hints on the user's real game revealed the last face-down card, auto-complete took over and recorded a real win. Fine for the app (correct behavior), but future device sessions that must preserve the user's game should not follow hints to the end on it (or should scrub back afterwards — not possible once won).

Feedback round 2 — F8 (2026-07-23):

- **Pulsing source vs static target can invert hierarchy for an instant**: with source opacity pulsing 1→0.62 and the target constant at 0.75, a screenshot (or glance) at the pulse's low point showed the roles nearly equal. Rule: the loud element's dim floor must stay above the quiet element's constant opacity (now 0.72 vs 0.62).
- Pulse implemented as an INFINITE reanimated loop sized to 2 cycles per 2.5 s window, not `withRepeat(…, 2)`: a repeat hint press extends the highlight window past 2.5 s, and a fixed 2-cycle pulse would die mid-window leaving a static ring.
- Ghost replay-on-repeat-press needs no nonce plumbing: every press re-solves and `parseSolverResponse` builds a fresh hint object, so keying the ghost effect on the hint object's identity replays it for free (and stays inert across unrelated re-renders).
- RN 0.86 dropped `StyleSheet.absoluteFillObject` from the TS types — use `StyleSheet.absoluteFill` in a style array instead.
- **Automating "is the amber ring showing?" on screenshots**: ffmpeg `colorkey=0xFFB020:0.14`→`alphaextract`→`signalstats` YAVG on a slot crop gives ~226 (ringed) vs 255 (clean) — used to walk draw-hint streaks unattended until a move hint appeared. Same idea located the ghost in recordings (plain YAVG spike on a felt-band crop the flight crosses).
- `screenrecord` VFR frame numbers (`-frame_pts`) are NOT linear wall-time (frames encode only on change); locate events by content signals (luma/color spikes), not frame arithmetic. Bonus: a low unique-frame count is itself evidence of a static screen (animations-off check: 8 frames/5 s vs 88+ with pulse+ghost).
- The settings screen exposes almost no a11y nodes on Android (12-node snapshot, switches unlabeled) — toggling a switch needs a screenshot + coordinate tap. Fine for a one-off; if settings toggling becomes routine in device tests, add testIDs/labels to the switch rows (or extend `?set=` — animations would have been handy this run).

Feedback round 2 — F9/F10 (2026-07-23):

- **`gen_moves::<true>` dominance forcing would break F9's candidate list**: with dominance on, any provably-safe move (buried-ace deck play, forced pile→stack) becomes the node's ONLY generated move — exactly on the draw-streak positions the feature targets. `gen_moves::<false>` is the correct enumeration source; the sub-solves themselves still run the normal dominance search (fast).
- **`SolitaireEngine<NoPruner>` is the sanctioned single-move applier** — no new vendor patch needed for F9. It re-validates against the same non-dominance mask the candidate came from, so `do_move` can't fail for generated candidates (debug_assert'd anyway).
- **`StandardMove` derives neither `Debug` nor `Clone`** — tests must rebuild it field-by-field (`Pos`/`Card` are Copy) and use `assert!(a == b)` instead of `assert_eq!`.
- Board moves that lose winnability are real, not theoretical: at seed 0 draw 1 move 59, the only no-draw move (K♠ waste→empty column) provably makes the game unwinnable — the verify-per-candidate design is what stops the hint from suggesting it. Scan rate: 580 of 1493 draw-first positions (39%) had a verifiable board alternative.
- **Hint-following terminates**: every candidate class consumes a finite resource (foundations grow, hidden cards flip, deck shrinks — digs and non-revealing shuttles are never hinted); the follow-hints-to-win test walks a full game in well under 400 hints.
- The F9 determinism caveat: results are deterministic except when a verification sub-solve lands near its budget slice boundary (Terminated on one run, Solved on another). Irrelevant for sub-ms mid-game solves; worth remembering for pathological positions.
- `git checkout -- <untracked file>` errors harmlessly ("pathspec did not match") — reverting an untracked file needs a real content edit (the F10 staleness proof used sed; fingerprint equality then proved byte-identical restore).
- The ensure fast path costs 33 ms (hashing ~40 files / ~1 MB) — cheap enough to sit in every build entry unconditionally. Warm full rebuild of all 4 targets: ~12 s (cargo warm), ~4 s per staleness-probe rebuild after a comment-only change.

Feedback round 2 — F11 (2026-07-23):

- **Reachable-stock simulation reduces to two passes over one "entry order" list**: cards surface as waste tops in the order they ENTERED the waste (the waste array is append-only between recycles, and recycling flips it so its oldest entry is drawn first), so pass 1 = the remaining stock in draw order, pass 2 = current waste ++ pass 1, each grouped by drawCount with the group's LAST card as the visible top. No stateful (stock,waste) simulation loop needed; a first cut with index arithmetic over a combined array was wrong precisely because drawn groups land REVERSED on the waste.
- **Draw-N phase shift happens across recycles, not within a pass**: playing the waste top removes an entry, which regroups every LATER group on the next recycled pass (earlier groups are unaffected). A test fixture for "card only reachable after a waste play" therefore needs the enabling play to sit mid-pass (entries after it shift), not at the tail. Pinned in usefulMoves.test.ts.
- **@expo/ui `ListItem` must not be used as a `FieldGroup.Section` row on Android**: FieldSection.android wraps EVERY row child in a Compose ListItem (position-aware rounded shape) — nesting the universal ListItem produces ListItem-in-ListItem. Compose rows also measure unweighted children first, so a long description Text would squeeze the trailing Switch to zero width; fix = Compose `weight(1)` modifier on the text column (weighted children measure last), iOS needs only a flexible Spacer. Both in `DescribedSwitchRow` with comments.
- **`Switch` labels are single-line only in @expo/ui 57** — no subtitle/description support on the component; a description row must be composed by hand (the animation/statistics descriptors have carried unrendered `description` fields since Phase B for exactly this reason).
- **Deep-link presses on freshly re-rendered boards need fresh snapshot refs**: two label-based `press` calls failed mid-session because the board re-rendered between snapshot and press (hint rings, plays); coordinates from stale refs tap the wrong node silently. Rule: snapshot → press @ref immediately, re-snapshot after every board change (label selectors are fine only for stable chrome like `id=stock`/`id=hint-button`).
- **Stuck-position solver proofs are the cheapest solve class seen yet**: played-out dead positions proved unsolvable in 0.6–3.4 ms visiting 1–3 states (A065, 21-solve session) — the empty stock leaves the search almost nothing to expand. The 800 ms background budget is ~250x headroom.
- **Organic stuck positions are fast to reach at draw 5 + solvableOnly:off**: first attempt hit heuristic-dead at the very first recycle-flip (~7 min incl. driving overhead) — no test fixture needed; reachability is sparse at draw 5, so most random deals grind down quickly.

Feedback round 3 — F13 (2026-07-24):

- **Exact root cause of "hint visuals behind cards" (both symptoms, both platforms, verified in RN 0.86 source)**: Fabric flattens "layout-only" views and z-sorts what remains. (1) `ViewShadowNode.cpp`: `pointerEvents="box-none"` does NOT form a stacking context (only `box-only`/`none` do) → AbsoluteCardLayer's absoluteFill wrapper is flattened away, hoisting all ~52 card views into the board shell's native child list. (2) `sliceChildShadowNodeViewPairs.cpp`: that combined child list is stable-sorted by `ShadowNode::orderIndex` = the view's `zIndex` (or 0). The HintOverlayLayer root (`pointerEvents="none"` → forms a view, no zIndex → orderIndex 0) therefore sorted BELOW every card (zIndex 100–~1700 at rest, 10000+ mid-flight) despite being the later JSX sibling. "Later sibling paints above" simply does not survive Fabric view flattening + orderIndex sorting. Android `elevation` was NOT involved (no card sets one; RN maps zIndex to drawing order, not elevation). The ordering code is shared C++ core → identical on iOS.
- Why this never showed before F13: rings are OUTSET (band mostly outside the card bounds) so isolated-card hints looked fine — only OVERLAPPING neighbors (waste fan, stacked columns, foundation piles under the ghost path) revealed the wrong plane. And the celebration overlay's "later sibling works" precedent is a false analogy: `buildCardLayerItems` returns `[]` while a celebration is active, so that overlay never actually competes with card views.
- Stock-ring root cause was layer-structural on top of the above: it rendered inside TopRow's slot subtree — the structural row paints under the whole hoisted card plane, so the absolute stock card clipped the band's inner edge. Moved into HintOverlayLayer with rect from `resolveStockRect` (same layout registry).
- **The fix rides the flight mechanism, one band higher**: real flights already win the z-battle via the sibling `zIndex: 10000 + item.zIndex` boost inside the one flattened board plane (AbsoluteLayerCard settle boost). The literal "flight snapshot registry" from `docs/product/flight-snapshot-registry/` no longer exists (`src/animation/flightController.ts` was deleted with the AbsoluteCardLayer refactor) — its replacement IS this per-sibling orderIndex strategy, so `HINT_OVERLAY_Z_INDEX = 20000` on the overlay root is the same-mechanism reuse: rings + ghost paint above every card including mid-flight ones, on both platforms, by construction.
- **Layering constraint for future board work (warning-model/bubble agents take note)**: the board shell forms a stacking context only because it has `onLayout` (an event prop sets a ViewEvents bit → `formsStackingContext`); the bottom dock (Undo/Hint/`GameNoticeBubble`, zIndex 2–4) stays above the cards ONLY by being a later sibling OUTSIDE that context. Anything mounted INSIDE the board shell that must beat the cards needs zIndex > the 10000+ flight band (and > 20000 if it must also beat hint visuals). Do not "clean up" the board shell's onLayout without rechecking stacking.

Feedback round 3 — F14/F15 (2026-07-24):

- **The sticky era replaces three F11 mechanisms at once**: once "warning fired ⇒ block all evaluation until era exit" exists, the once-per-position dedupe refs (re-fire can't happen inside the era), the stuck-vs-unwinnable precedence check (one mode = one possible warning) and the bubble timer (persistence IS the point) all become dead code. Deleting them is safe BECAUSE re-warning after an exit-and-return is the wanted re-arm behavior, not a bug.
- **Effect declaration order is load-bearing in useHint**: the era-exit effect must run before the background-evaluation effect (same commit) so the evaluation never sees a stale era for a below-boundary position. React runs effects in declaration order — a comment pins this; don't reorder.
- **Fire-on-discovery closes the debounce gap**: the hint button's own unsolvable proof starts the era directly when the active mode would warn. Without it, a press in the 600 ms pre-background window shows a hint ring and then the warning — exactly the user-reported contradiction. The background handler ALSO re-checks the era at response time because its solve can be queued behind the button's (replace-latest queue = both run).
- **Era boundary > 0 on demand (device-test recipe)**: an unsolvable-from-deal-one game warns at depth 0, making "undo below the boundary" untestable. Deterministic trick: deal with `warnings:off`, make N moves, then switch the mode via `?set=` — evaluation arms at the CURRENT depth, so the era boundary lands at N. Used for the b-05/b-06 proof.
- **@expo/ui 57 Picker has no `modifiers` prop** (PickerProps: selectedValue/onValueChange/appearance/enabled/children/testID only) — you cannot weight the picker in a Compose row. With a wide menu button ("No more useful moves" sizes it) a weighted LABEL gets squeezed into mid-word wrap ("Warning\ns", evidence a-01). Fix: Android stacks label above picker inside the one ListItem child; iOS keeps the SwiftUI label-left row idiom (handles compression natively).
- **First hook-level jest suite in the repo**: react-test-renderer 19 (already in node_modules) + a null-returning harness component + `import { act } from 'react'` + `IS_REACT_ACT_ENVIRONMENT = true` works under jest-expo with fake timers; real reducer states drive positions (draws/undos/new deals) while `modules/soli-solver` and `usefulMoves` are jest-mocked — solve-call counts become directly assertable ("era ⇒ zero evaluations").
- **Warning-bubble presence oracle**: dock-band crop `ffmpeg crop=680:70:200:2000 signalstats` YAVG ≈ 69 with the bubble, ≈ 124 clean — a one-liner presence check that drove the undo-until-clear loop unattended.
- **`adb logcat -d` counts are only phase-local**: the ring buffer rotated mid-session (count "dropped" from 3 to 2). Count new lines per phase against the last-seen timestamp, or snapshot the lines at every checkpoint (EVIDENCE.md does the latter). A backgrounded `logcat | rg > file` pipe did NOT survive the shell tool's process cleanup — don't rely on it.
- **Shared-phone hazard is real**: mangoinventory (the invent repo's app) stole the foreground mid-session despite holding `/tmp/android-device.lock` (probably a heads-up notification; the lock only serializes agents, not the phone's own apps). Three taps landed in the wrong app (camera area + a nav tab — harmless). Detection: screenshot luma went nonsensical (208) → screenshot review → `dumpsys activity` confirmed. Recovery: `am start` Soli (process alive → in-memory era SURVIVED backgrounding, bonus proof c-02b). Rule of thumb: when an oracle value goes out of family, look at the full screenshot before trusting any further taps.
- **(e) Persistent-display judgment — full bubble kept, no compact variant**: across every captured board state (draw-1 and draw-5, fresh through played-out) the bubble floats over empty felt above the dock and never covered a card; a column would need ~18+ stacked cards to reach the bubble band (near-degenerate), the bubble is pointerEvents-none (never blocks interaction), and a second visual variant would be gold plating for an unobserved case. Revisit only if a real board shows occlusion.

- [resolved] jest round-trip test counted 51 cards mid-game — foundation counts are implied cards; test helper now expands them (mirrors Rust `build_state`).
- [resolved] pre-existing clippy `useless_format` warning in `protocol.rs` — fixed while touching the crate. One warning remains in vendored `lonelybot` (upstream code, deliberately untouched).
- [resolved, Phase C] iOS link failure `ld: library 'soli_solver_ffi' not found` (first `yarn ios` with the pod): the xcframework's simulator slice was arm64-only, but the app's Release-iphonesimulator target builds for (arm64 x86_64) — CocoaPods' `install_xcframework` copies a slice only if it covers ALL current build archs, so it silently skipped it (warning in build log: "Unable to find matching slice"). NOT a `@_silgen_name` problem. Fix: `scripts/build-rust-solver.sh` now also builds `x86_64-apple-ios` and lipo-merges both sim archs into one `ios-arm64_x86_64-simulator` slice; artifacts rebuilt, pod reinstalled. Sim `.a` is now 38 MB (two archs, doesn't ship to users; device slice unchanged 19 MB).
- [resolved, Phase C] `expo run:ios` did NOT auto-run pod install for the new local module (Podfile unchanged → its check misses new autolinked local modules): first build had no SoliSolver pod at all. Run `npx pod-install` manually after adding a local Expo module.
- [resolved, Phase C, machine-local] Both platform builds failed on stale node paths before anything compiled: Android Gradle daemons cached a dead env ("Could not start 'node'"; fixed via `cd android && ./gradlew --stop`), and gitignored `ios/.xcode.env.local` pointed at a deleted Homebrew Cellar path (`node/25.9.0_2`; fixed by setting `NODE_BINARY=/opt/homebrew/bin/node` — stable symlink that survives brew upgrades).
- [resolved, Phase C] Android: autolinking + jniLibs packaging worked first try — `libsoli_solver_ffi.so` (511 KB) verified inside the release APK (`unzip -l`).
- [open, cosmetic, dev-only] During demo playback with hints on, `DemoPlaylistHud` overlaps the Hint button (same left-half slot; HUD is pointerEvents-none, hint presses are no-ops during demo).
- [open, minor] Solver notice bubble and the undo discovery bubble can theoretically show at once (both anchor above the dock) — rare, accepted for v1.

## Testing

### Desktop perf (2026-07-13, Apple Silicon MacBook, single thread, release build, 5000 ms budget, via `cargo run --release --example perf -p soli-solver-ffi`)

150 solvable + 50 unsolvable fresh `default` seeds per draw count; mid-game = every winning-line position for 30 seeds, every 5th for the rest (7.7k–13k positions per draw count). All solves go through the full JSON FFI path.

| draw | fresh solved P50/P90/P99/max ms | fresh unsolvable P50/P90/P99/max ms | mid-game solved P50/P90/P99/max ms | unknown |
|---|---|---|---|---|
| 1 | 0.04 / 2.0 / 319 / 837 | 22 / 144 / 813 / 813 | 0.022 / 0.13 / 19 / 684 | 0 |
| 3 | 0.05 / 2.4 / 366 / 818 | 0.8 / 69 / 1379 / 1379 | 0.022 / 0.10 / 6.7 / 917 | 0 |
| 5 | 0.075 / 2.0 / 58 / 80 | 0.04 / 1.9 / 32 / 32 | 0.021 / 0.10 / 1.4 / 106 | 0 |

Takeaways:

- Mid-game hint solves (the product case) are P50 ~0.02 ms, P99 ≤ 19 ms, worst ~0.9 s. Even 5x slower on a phone: nearly always imperceptible; rare sub-second worst cases within a 1.5 s budget.
- Unwinnable proofs on fresh deals are the heaviest class (P99 ~1.4 s desktop) but still bounded; zero budget-exhausted (`unknown`) outcomes in ~30k solves at 5 s.
- Visited-state counts correlate: P50 ~100 states mid-game, worst ~12–20 M (that's where the max times come from).

### Desktop perf with F9 board-first verification (2026-07-23, same harness/machine class, budget 5000 ms)

Every solved request now also pays for candidate verification (a few sub-solves). Mid-game solved rows vs the 2026-07-13 baseline:

| draw | mid-game solved P50/P90/P99/max ms (F9) | baseline (07-13) | unknown |
|---|---|---|---|
| 1 | 0.024 / 0.175 / 30.9 / 1063 | 0.022 / 0.13 / 19 / 684 | 0 |
| 3 | 0.024 / 0.112 / 9.8 / 852 | 0.022 / 0.10 / 6.7 / 917 | 0 |
| 5 | 0.023 / 0.118 / 1.7 / 128 | 0.021 / 0.10 / 1.4 / 106 | 0 |

Added cost: ~+10% at P50 (≈ +0.002 ms), P99 up ~1.5x but still tens of ms; maxima remain in the same sub-second class and the budget slicing caps any single candidate at 200 ms. Zero budget-exhausted outcomes across ~30k solves. Fixture-level: the 3041-position scan saw max 0.8 ms total per hint request.

### On-device perf

iOS simulator (2026-07-13, iPhone 17 Pro Max sim on Apple Silicon host, Release, arm64 — NOT phone-representative for absolute CPU, but validates the bridge overhead is negligible; times are the Rust-internal `solveMs`):

| source | n | min ms | median ms | max ms |
|---|---|---|---|---|
| hint (button, budget 1500) | 20 | 0.0 | 0.1 | 0.1 |
| auto (background, budget 800) | 35 | 0.1 | 0.3 | 3.5 |

All 55 solves: median 0.2 ms, max 3.5 ms (first solve of the session, visited=20081 — warm-up). Statuses: 52 solved, 3 unsolvable (draw-5 deal), 0 unknown/invalid/error. Positions were fresh/early-game draw-1 plus a draw-5 unwinnable deal and the near-win fixture; visited 1–20081.

Android phone (2026-07-23, A065 = Nothing Phone (2), Snapdragon 8+ Gen 1 (SM8475), Release APK, times are Rust-internal `solveMs` from `adb logcat`):

| source | n | min ms | median ms | max ms |
|---|---|---|---|---|
| hint (button, budget 1500) | 13 | 0.2 | 0.5 | 1.0 |
| auto (background, budget 800) | 18 | 0.5 | 0.9 | 2.5 |

All 31 solves: median 0.7 ms, max 2.5 ms. Statuses: 29 solved, 2 unsolvable (draw-5 random deal), 0 unknown/invalid/error. Positions: real mid-game (user's 33-moves-left game walked down to 28), two fresh draw-1 solvable deals (90/91-move lines), one unsolvable draw-5 deal. Comfortably inside budgets — the background debounce (600 ms) dominates end-to-end latency, not the solve.

F11 addendum (2026-07-23, same phone, 21 solves): the stuck warning's confirmation solves on played-out dead positions are the cheapest class yet — 0.6–3.4 ms, visited 1–3 states (empty stock leaves the DFS almost nothing). F9 board-first hint solves on a fresh draw-1 deal: 0.4–0.7 ms at visited=97 (sum incl. verification sub-solves). Zero unknown/invalid/error, zero `[stuck-heuristic]` false positives.

### F13 z-order device pass (2026-07-24, A065, Release, draw-3 solvable deal, evidence `.test-artifacts/hints-f13/`)

Cheap gates first: typecheck + lint + jest all green (32 suites / 324 tests, +2 `resolveStockRect`). Deliberate overlap scenarios, all constructed organically by following hints:

- (a) Waste-fan ring over neighbors: `a-waste-fan-ring.png` (A♥ hint — ring band fully over the adjacent 8 in the 3-card fan), `a2-waste-fan-ring-with-ghost.png` (3♣ over K neighbor). Pre-fix this band vanished under the middle fan card.
- (b) Mid-column source ring above ALL column cards: `b-tableau-ring-and-ghost-arrival.png` (10♠ ring outset drawing over the face-down card above it; also `hint03`/`hint04` — 6♦ under face-downs, run-spanning J♦+10♠ ring).
- (c) Ghost over a stacked foundation at arrival: `c-ghost-over-stacked-foundation.png` (ghost 3♥ ON TOP of the 2-deep A♥+2♥ hearts foundation; recording `f13-ghostc.mp4`).
- (d) Ghost over intermediate columns mid-flight: `d-ghost-crossing-columns-1/2.png` (J♦ ghost crossing columns 2–4 above their face-up cards; recording `f13-ghost3.mp4`), `d-ghost-over-dest-column.png` (4♣ over the destination column's cards, `f13-ghost1.mp4`).
- (e) Stock ring both states after the layer move: `e1-stock-ring-cardback.png` (card-back stock) + `e2-stock-ring-recycle.png` (empty stock / recycle icon).
- (f) Regression: timer-clear (`hint02b` — rings gone at ~2.6 s, board unchanged), instant interaction-clear (`f-after-interaction.png`, stock-crop amber YAVG 255 right after the tap), hint-button flicker still absent (screenrecord across two draws: button-crop YAVG spread 0.115/255), a11y announcement path untouched by the diff (`getHintAnnouncement`/`applyHintHighlight` unchanged; strings pinned by unit tests).
- Amber-ring automation reused from F8: stock-slot crop + `colorkey=0xFFB020` → YAVG ~220 ringed vs 255 clean, drove the draw-follow loop unattended.
- Session notes: settings restored to stuck-warning-era defaults afterwards (draw 1, autoUp on, solvableOnly on, stuckWarning on, hintButton off, unwinnableWarning off — via `?set=` + `--screenshot` so dev mode ends OFF) and a fresh deal left on screen. Known pre-existing quirk (also seen before the fix, not F13-related): each `yarn deeplink` delivery to the A065 leaves ONE stray draw on the fresh board (visible as a single waste card at moves 0); left as-is, one Undo clears it. agent-device sessions on this run died between shell invocations (SESSION_NOT_FOUND) — re-`open` per batch or use raw `adb input tap` for fixed chrome (hint button 269,2150; stock 1001,716 on 1080x2412).

### F14/F15 warning-model device pass (2026-07-24, A065, Release, evidence `.test-artifacts/hints-f14/` — line-level log map in its EVIDENCE.md)

Cheap gates first: typecheck + lint + jest green (33 suites / 353 tests; +15 hook-level warning tests, settings matrix, link-alias truth table).

- (a) Migration + picker: the phone's real round-2 payload (stuckWarning on / others off) landed on warningMode 'No more useful moves' with hint button OFF (a-01) — the F11→F14 chain live; the legacy hintsEnabled chain is matrix-covered in jest. Picker renders all three spec labels, opens, selects, persists (a-02..a-04). One on-device iteration: the initial one-line row wrapped the label mid-word ("Warning\ns") → Android two-line layout shipped (see learnings).
- (b) Mode 'unwinnable' on a dead draw-5 deal (solvableOnly off): warning fired once (auto solve 01:38:40, era boundary depth 2 via the warnings-off-then-switch recipe), PERSISTED across 3 draws including a recycle with ZERO further `[solver]` lines (b-02/b-03), hint press re-affirmed (no ring, no solver call — count unchanged, b-04), undo below the boundary cleared it (b-05) and evaluation resumed immediately (new auto line 01:40:53 → re-warn at the shallower depth, b-06).
- (c) Mode 'noUsefulMoves' on the same dead deal: the mode switch itself cleared the live unwinnable era (c-01, bubble luma 69→124, no solve mid-stock); stuck warning fired exactly at the recycle-flip (confirmation solve 01:42:46, c-02) and persisted across the recycle + further draws + a hint press with zero additional solver lines (c-02b/c-03/c-04 — the press re-affirmed the stuck text, never "No hint found."); undo walked back inside the era 4× (warning held) and cleared on the 5th (below the flip boundary, c-05), after which mid-stock evaluation stayed silent (classic semantics).
- (d) Mode 'off': dead fresh deal + draws produced zero `[solver]` lines; hint press on the lost game gave the classic fallback (`source=hint status=unsolvable` → "No hint found." mid-stock, d-01); on a solvable draw-1 deal the press produced the winning board-first hint (status=solved visited=89, A♠→foundation rings, d-02).
- (e) Obstruction judgment: full bubble kept — reasoning + revisit trigger in learnings.
- (f) Restored: Draw 1, solvable ON, warnings 'No more useful moves', hint button OFF, dev mode OFF, fresh deal left (f-01; the known stray-draw quirk left one waste card at moves 0, as in F13).
- Session incidents: the shared-phone foreground steal by mangoinventory (detected + recovered, see learnings; era survived app backgrounding — c-02b) and logcat ring-buffer rotation (mitigated by per-checkpoint line snapshots).
- Perf note: all warning-path solves this session were 0.2–1.2 ms at visited=1–89 — the sticky era makes the steady-state cost of a dead game exactly zero solves.

## Feedback round 1 (2026-07-23) — work items

From the user prompt above (statuses inline):

- [x] F1. **Hint button flickers on every move** — FIXED (2026-07-23). Root cause confirmed: `useHint.drainSolveQueue` toggled `setSolverBusy(true/false)` around EVERY solve — including the debounced background unwinnable check after each move — and `useKlondikeGame` fed that into `hintDisabled: solverBusy || boardLocked`, which switched the Hint pressable's `disabled` prop + a dimmed-opacity style for the 1–2 frames a sub-ms solve takes. Fix: deleted the `solverBusy` React state entirely (busy bookkeeping lives in refs; also saves two tree re-renders per background solve) and removed the `hintDisabled` prop/style from `UndoScrubber` — the button is now visually static; presses during an in-flight solve are absorbed by the replace-latest queue (a repeat solve of the same position just restarts the ring timer, which is the wanted repeat-tap behavior anyway). `boardLocked` needed no replacement: locking only happens in win/celebration contexts where the dock is hidden (`shouldShowUndo` and the hints-on visibility term both check `hasWon`/`celebrationState`). No React.memo added — the dock subtree re-renders per move regardless (historyIndex in the a11y label) but now renders an identical element tree, so no native update occurs.
- [x] F2. **Hint highlight polish** — DONE (2026-07-23). Replaced the selection-dispatch approach with a dedicated overlay: new `HintOverlayLayer` (later sibling of `AbsoluteCardLayer`, `pointerEvents="none"`, mounted only while a move hint is active) renders a source ring + target ring; `useHint` is now fully dispatchless (SELECT_*/CLEAR_SELECTION pathway deleted; the engine's selection actions remain — nothing else in production dispatches them, they're reducer/test surface). One unified `HintRing` visual (3 px `COLOR_DROP_BORDER` ring, 3 px outset, subtle glow) shared by source, target AND the stock draw ring in TopRow (testIDs `hint-source-ring`, `hint-target-ring`, `hint-stock-ring`). Tableau source rings enclose the WHOLE moved run (hint card = run base; a base-card-only ring would have run cards crossing its border). Rects come from the same layout registry + stacking math the card layer uses (`resolveTopRowPosition`/`resolveTableauPosition`/`computeTableauStackOffsets`/`computeWasteFanGeometry`, now all in cards/utils.ts). Draw + move hints share one highlight state with a 2.5 s timer (repeat taps restart it); any committed board change clears instantly (position-key render gate) and permanently (effect clears state, so scrub-away-and-back can't resurrect a ring). A11y: `AccessibilityInfo` announces "Hint: move Seven of hearts to column 3." / "… to the foundation." / "Hint: draw from the stock." Invalid taps (wiggle, no state change) deliberately do NOT clear the rings — the hint is still valid; every real action changes the position key and clears.
- [ ] F3. **Hint flow options (prefer board moves over draw streaks)** — options presented to user first (see chat 2026-07-23); implementation pending user pick. Candidate: verify-with-solve prefer-board hint (enumerate current non-draw progress moves, re-solve after each, hint the first that stays winnable; else follow solver line/draw).
- [x] F4. **Split into three settings** — proposal presented to user (see chat): (1) Hint button, (2) classic "no more moves" heuristic warning, (3) solver-proven "unwinnable" warning (the current bubble; changes gameplay per user feedback — must not be forced on hint users). Research on classic heuristics commissioned (how apps detect it, faultiness, exact algorithm). RESOLVED by F11 (2026-07-23) — implemented there.
- [ ] F5. AGENTS.md correction (engine in `src/solitaire/`) — [x] done 2026-07-23.
- [ ] F6. Artifacts commit decision — user: commit later, NOT yet; sim-slice explanation delivered in chat first.
- [x] F7. Android functional/perf pass (C2/C4) + F1/F2 device verification — DONE (2026-07-23), see C2/C4 and "On-device perf". F1 flicker verified fixed with pixel evidence: 1080p screenrecord across recycle+3 draws (each triggering a background solve), per-frame luma of the Hint-button crop spans 227.48–227.62 / 255 (encoder noise; the old disabled-opacity blink would dip it by dozens). F2 verified for every hint class with screenshots in `.test-artifacts/hints-f1f2/`: source+target rings (single card 02/04/06, 3-card run 04, waste top 14, ace→empty foundation slot 20), draw ring on stock card back (11/12) and on empty-stock recycle slot (10), 2.5 s auto-clear (07), instant clear on user draw (08→09). Session note: following hints on the user's real in-progress game revealed its last face-down card and the Auto Up setting auto-completed it — recorded as a REAL win (256 moves) in history; deliberate re-deals afterwards. Settings restored to their observed pre-test values (Draw 1, solvable on; hints/dev-mode were already on before the session).

## Feedback round 2 (2026-07-23) — work items

- [x] F8. **Hint highlight redesign** — implemented + full Android device pass green (2026-07-23). Problem: unified green rings hard to see (drop-border green blends with the felt, even read as blue) and direction-ambiguous (card→foundation hint read as foundation→card). Shipped design (all in `HintOverlayLayer.tsx` + `COLOR_HINT` in constants.ts):
  - **Role-distinct amber rings**: SOURCE = loud — `COLOR_HINT` #FFB020, 3 px border, 3 px outset, amber glow + 1 px near-black contours on BOTH sides of the band (outer spread + inset boxShadow — the cheap contrast fix against white faces and bright felt), gentle pulse (opacity 1→0.62, scale 1→1.03, 1.25 s/cycle = 2 cycles per 2.5 s window, infinite loop so repeat-press extensions keep pulsing). TARGET = quiet — rgba(255,176,32,0.75), 2 px, static, dark contour only. Amber over reusing FOUNDATION_GLOW_COLOR/tamagui $yellow: pure yellows wash out on white card faces; board palette is hex constants, not tamagui tokens.
  - **Ghost glide (direction cue)**: one-shot translucent copy of the hinted card's face (reuses `CardVisual` — no new card rendering) + 2 px amber outline, card-sized, glides source→target in 520 ms `Easing.inOut(cubic)`, fade-in 90 ms (starts pixel-aligned over the real card → reads as "lifting off"), fade-out 180 ms on arrival, max opacity 0.7. Replays on repeat Hint press (keyed on the fresh hint object per solve — doubles as press feedback). Rendered above the rings, pointerEvents none, testID `hint-ghost`.
  - **Draw hint**: stock ring in TopRow uses the same source treatment (role default 'source') — the stock IS the action target. No ghost for draws.
  - **Animation gating**: ghost behind `cardFlights` sub-toggle (it mimics a card flight; a player who disabled Card flights asked for no cards flying — master-off implies it via useAnimationToggles). Pulse behind `master` only (stationary emphasis, not a flight; a dedicated hints sub-toggle would be scope creep). Animations off → static role-distinct amber rings.
  - **Kept**: 2.5 s auto-clear, instant clear on board change, repeat-tap timer restart, testIDs `hint-source-ring`/`hint-target-ring`/`hint-stock-ring`, a11y announcements, useHint untouched.
  - **Structural**: `resolveHintRects`/`HintRect` moved HintOverlayLayer→cards/utils.ts (overlay now imports CardVisual→tamagui, which jest can't load; resolver must stay unit-testable). Tests import from utils.
  - **Alternatives considered (not built)**:
    - *Arrow/chevron overlay pointing into the target*: + explicit direction even as a static image (works with animations off); − new iconography on the board (visual noise, style to design/maintain), arrows crossing card art read badly on dense boards, still needs role-distinct endpoints to anchor it.
    - *Numbered 1→2 badges*: + unambiguous order, trivially static; − reads as tutorial UI, adds text/badge rendering over cards, localization/scale concerns for one transient cue, still two similar-looking marks to parse.
    - *Sequential blink (source blinks, then target)*: + zero new visuals, direction via time; − direction only readable if you watch the full sequence (missable, slower to parse than motion), worse for reduced attention, and with animations off it degrades to the ambiguous static pair anyway.
    - *Static two-color role scheme (no motion)*: + simplest, animation-independent; − colors alone don't encode direction (which color means "from"? — needs learning), and two loud colors on the board fight each other. It IS our animations-off fallback, but as loud/quiet same-hue roles instead of two hues.
    - *Why ghost+role-contrast won*: direction is shown by literally previewing the move (self-explanatory, no iconography to learn), reuses the app's existing card-face rendering and animation stack, and degrades gracefully (loud/quiet rings) when motion is off.
  - **Device verification (A065, Release build, 2026-07-23)** — evidence in `.test-artifacts/hints-f8/`:
    - All hint classes screenshotted and inspected via zoom crops: tableau→foundation ACE case (04 + crop-04-*; the user's original misread case — loud ring on A♣, quiet on foundation), tableau→tableau (09: 2♦→3♣), waste→tableau (scrubwalk-6: 4♦→5♣; scrubwalk2-3: 3♣→4♦), draw/stock (05/07), bonus waste→foundation (walk-3: A♥).
    - Ghost glide verified as readable source→target motion by frame-stepping screenrecords (ghost-filmstrip, ghost-path-seq*): fade-in over the source card ("lifts off"), decelerating arrival into the target ring. Repeat-press replay proven — two glides in one recording (ghost-repeat-tap.mp4, felt-band luma spikes at both presses + mid-flight frames).
    - One visual iteration after the first pass: source border 3→4 px, pulse dim floor 0.62→0.72, target alpha 0.75→0.62 — first-pass stills showed the mid-pulse source momentarily reading as weak as the static target (hierarchy inversion for an instant); after the tweak, crops show unambiguous loud/quiet roles.
    - Flicker regression: Hint-button crop luma spread 0.174/255 over 31 frames spanning 3 draws (encoder noise) — button stable.
    - Clear-on-interaction: ring gone on the very next frame set after a stock tap (06); rings also cleared correctly after following the ace hint (05).
    - Animations-off branch: master toggled off via Settings UI → same rings, static (ring-crop luma constant 247.24 across all ring frames; only 8 encoded VFR frames in 5 s vs 88+ with animations on = no pulse, no ghost), screenshot 11. Master restored ON afterwards; final smoke press re-verified pulse+ghost live (12).
    - Settings left as found: Draw 1, solvable on, hints on, dev mode on, all animations on. Board left on the disposable scrubtest demo fixture (session started from a test deal, no real game touched).
- [x] F9. **Board-first hint flow (Option 1)** — DONE in Rust (2026-07-23; device verification pending). Implemented entirely in `rust/soli-solver-ffi/src/solve.rs` (FFI contract unchanged, TS untouched, zero new vendor patches):
  - **Candidate enumeration**: `board_first_candidates` uses `gen_moves::<false>` — the non-dominance variant is load-bearing: default `<true>` generation collapses to a single forced move whenever any dominance-safe move exists (e.g. buried ace in the deck, state.rs:136-206), which would empty the candidate list exactly on the draw-streak positions F9 exists to fix. Priority order (deterministic; mask iteration is ascending card index): a. `PileStack` (tableau→foundation; covered card expands to a pile→pile pre-move via convert — still no-draw), b. `DeckStack` with card == `deck.peek_current()` (waste-top→foundation), c. `Reveal` (tableau run moves incl. king-to-empty), d. `DeckPile` with card == waste top. Excluded: `StackPile` (foundation digs) and deck moves needing draws.
  - **Candidate application**: `SolitaireEngine::<NoPruner>` (public lonelybot API) — validates against the same non-dominance mask the candidate came from, so no new SOLI PATCH was needed (`Solitaire::do_move` stays pub(crate)).
  - **Verification**: per candidate, fresh sub-solve on the one-move-deeper clone with budget slice = min(200 ms, half the remaining request budget) — slices telescope, so total stays within the request budget; `Terminated` counts as not-verified and moves on. Shortcut: a candidate equal to the main line's first move is proven by the main line itself (no sub-solve, immune to spurious timeouts). Everything (main solve + verification) runs on the one 4 MiB-stack solve thread.
  - **Response semantics** (documented choices): `winMovesRemaining` stays the MAIN line's primitive length (the candidate's own optimal length is unknown — its sub-solve only proves winnability); `solveMs` = total request time incl. verification; `visited` = SUM of main + all verification sub-solves (one shared counter; matches solveMs covering the whole request).
  - **Fixtures/tests** (tests/solver_tests.rs, 6 new, 19 total green): scan of 3041 winning-line positions found 1493 draw-first raw lines of which 580 (39%) now hint board moves; pinned fixture A (seed 0 draw 1, 1 move in: raw line draws, hint must be 7♦ col3→col0, winnability-after-move asserted), fixture B (seed 0 draw 1, 59 moves in: the ONLY no-draw candidate — K♠ waste→empty column — provably makes the game unwinnable, hint must stay draw; the verified-fail path), and a waste-top fixture (seed 0 draw 1, 11 moves in: raw line draws but waste-top 6♠→7♦ must be hinted — pins candidate classes b/d, whose silent loss would just look like extra draw hints). Plus determinism (same request twice → identical status/hint/winMovesRemaining/visited), follow-hints-to-win walk (fresh seed-0 draw-1 deal, every hint legal, wins < 400 steps, board-move hints present), and perf sanity (< 500 ms bound; observed max 0.8 ms over the scan).
  - **Perf** (host, same harness as 2026-07-13 baseline, now incl. verification on every solved request): mid-game solved P50 0.024 ms (was 0.022), P90 0.175 (0.13), P99 30.9 (19), draw-3/5 similar; zero unknown across ~30k solves. Cost per hint ≈ a few extra sub-ms solves.
  - **Artifacts rebuilt** with F9 via the new ensure script (F10). Device verification: a later agent should check that fresh draw-1 deals now open with board plays instead of ~18-draw streaks.
- [x] F10. **Artifacts in git: research best practice, then decide + apply** — DONE (2026-07-23). Research + decision: see "Artifacts-in-git decision (2026-07-23)" below. Applied per spec:
  - `.gitignore`: jniLibs/ + SoliSolver.xcframework/ + `.artifacts-fingerprint.json` ignored (with keep-in-sync-with-.easignore note); verified via `git check-ignore` (artifacts ignored, module sources not) and `git status` (only the 8 module source files remain addable).
  - New `scripts/ensure-rust-solver.js` (Node stdlib only, exports `ensureRustSolverArtifacts()`): sha256 over sorted rust/ files (excl. any `target/` dir + `.DS_Store`) + `scripts/build-rust-solver.sh`, path+NUL+bytes+NUL per file; compared to `modules/soli-solver/.artifacts-fingerprint.json` `{fingerprint, builtAt, rustcVersion}` AND all four artifacts must exist. Match → silent return (measured 33 ms). Stale/missing → rebuild via build script when cargo+rustup exist (fingerprint written only after verified success), else throws the loud brew-install/rustup-init instructions. rustcVersion recorded for forensics only, never gates (a plain `rustup update` must not force a 4-target rebuild).
  - Wiring: called in `scripts/build-install-android.js` and `scripts/build-install-ios.js` mains right after the build lock (two entries must never cargo-build concurrently; iOS before pod install sees the vendored framework), prepended to package.json `build:ios`, and — spec adaptation, per the decision's own "every build path" rationale — also in `build-android.sh` (Play Store AAB; a stale solver in a store bundle is the worst staleness).
  - Root `.easignore` added: mirrors `.gitignore` minus the artifact lines, header documents the replace-not-merge semantics, the keep-in-sync rule, `.env` staying secret, and the known EAS-GitHub-integration limitation (fails loudly at pod install, never silently stale). `eas build:inspect` verification deferred to the next real EAS build (network + account operation).
  - Proofs: (1) first ensure run detected "no fingerprint recorded yet", rebuilt all 4 targets WITH F9 in 12 s warm, wrote fingerprint; (2) rerun = silent exit 0 in 33 ms; (3) appending one comment char to lib.rs → stale → rebuild → new fingerprint; reverting → stale → rebuild → fingerprint byte-identical to the original (content-determinism of the hash proven); (4) `git status --porcelain` free of all artifact paths; (5) `yarn release` entry ran lock→preflight→signing→ensure (silent pass)→device discovery→gradle start, then deliberately stopped before real build work.
- [x] F11. **Classic "no more useful moves" warning + three settings** — DONE (2026-07-23): implemented, unit-tested (gates green: typecheck/lint/jest 32 suites/322 tests, +29 tests) and full Android device pass on the A065 (evidence `.test-artifacts/hints-f11/`). Research had landed earlier in "Classic no-more-moves heuristic research" + "Microsoft-style stuck warning — deep dive" above; implementation follows the recommended Soli spec with the user's recycle-flip display timing (Option B semantics via a hard stock-empty gate). What shipped:
  - **Three independent settings** replacing `hintsEnabled` (settings.tsx `hints` group, mirroring the statistics-preferences pattern): `stuckWarning` ("No more moves warning", default ON — genre norm), `hintButton` ("Hint button", default OFF), `unwinnableWarning` ("Unwinnable warning", default OFF — the "plays very differently" mode). Settings screen renders them as label+description rows via new `components/settings/DescribedSwitchRow.tsx` (Row/Column/Text/Switch primitives — NOT the universal ListItem: FieldGroup.Section on Android already wraps each row in a Compose ListItem, nesting another would double-wrap; Android needs a Compose `weight(1)` modifier on the text column so long descriptions wrap instead of squeezing the switch out).
  - **Migration**: legacy `hintsEnabled: true` → `hintButton` + `unwinnableWarning` both ON, once, read straight off the parsed payload inside `mergeSettings` (explicit new-shape keys win; next settings write drops the stale key; population = one dev device, so no versioned machinery). Verified live on the A065: pre-install state had hints on → post-install settings showed stuck ON (new default) + button/unwinnable ON (`a-01`), then deep-link reset to defaults (`a-02`).
  - **Classic stuck warning**: new pure-TS `src/solitaire/usefulMoves.ts` — `findFirstUsefulMove` (priority-ordered, returns a `SolverHint` so the button fallback reuses the highlight pipeline 1:1) + `hasUsefulMove` + `collectReachableWasteTops` (draw-N phase-correct two-pass reachable-stock simulation; entry-order model, ≤2 passes). Enumeration: tableau→foundation, waste→foundation, waste→tableau (incl. K→empty), revealing/column-freeing full-run moves (king-runs with nothing beneath skipped), partial splits only when the exposed card is foundation-ready, then the reachable-stock draw check. Digs + pure rearrangements excluded (Microsoft-compatible). Klondike rule helpers `canDropOnTableau`/`canDropOnFoundation`/`KING_RANK` exported from klondike.ts so legality can't drift.
  - **Trigger semantics** (in `useHint`, one merged background effect for both warnings): evaluated on the debounced position change but ONLY at the recycle-flip moment (`stock.length === 0`; mid-stock not even the µs heuristic runs — no mid-deck oracle) and only warned after a solver confirmation (`status=unsolvable`, 800 ms budget) on the same position key; `solved` → suppress + `console.warn('[stuck-heuristic] false positive …')` telemetry; `unknown`/`invalid`/`error` → suppress. Re-arms per position key (any move/undo clears; re-warns at each fruitless flip — MobilityWare-style repeat), bubble copy "No more useful moves — undo or start a new game." (weak framing per faultiness verdict).
  - **Interplay dedupe**: `unwinnableEraRef` marks exactId+history-depth when the unwinnable bubble fires; while marked (cleared on undo/scrub below that depth or new deal) the stuck bubble stays silent — one bubble per dead era, the stronger warning wins.
  - **Gating rewire**: background per-move unwinnable check runs ONLY with `unwinnableWarning` on; stuck evaluation only with `stuckWarning` on (default-ON setting costs zero background solver work during normal play — proven on device: an entire dead draw-5 pass produced 0 auto solves, the flips 1 each); Hint button visibility/press gated on `hintButton` alone.
  - **Hint button fallback**: on `unsolvable` OR `unknown` the button now hints `findFirstUsefulMove` with the normal F8 visuals (deliberately identical handling so the button never leaks winnability — the old unsolvable→unwinnable-bubble path is gone, that claim belongs to the opt-in warning); empty enumeration → notice bubble ("No more useful moves…" at stock 0, else "No hint found.").
  - **Bubble rename**: `UnwinnableBubble` → `GameNoticeBubble` (variants are text-keyed, one visual; testID stays `hint-bubble` so device recipes keep working).
  - **Deep links**: `?set=` learned `stuckWarning|hintButton|unwinnableWarning:on|off`; `hints:` kept as alias for `hintButton` (skill table updated).
  - **Device pass** (A065, Release, 2026-07-23, `.test-artifacts/hints-f11/`): (a) migration + defaults screenshots incl. new settings rows rendering with wrapped descriptions; (b) button hidden by default (fresh-deal dock absent), appears via `hints:on` alias, F9 board-first confirmed live (first hints A♦→foundation then 2♦→foundation, `visited=97` incl. verification sub-solves, no draw streak); (c) organic draw-5 stuck reached in ~7 min — bubble EXACTLY at the recycle-flip (`c-01`), button fallback on the dead position shows the stuck notice (`c-02`), full dead pass mid-stock silent with zero solver calls (`c-03`), re-warn at the next flip (`c-04`), undo clears; (d) unwinnable stayed OFF-silent through (c) (only the 2 flip confirmations in the log), enabled → proactive bubble after a move pre-recycle on an unsolvable fresh deal (`d-03`), dedupe held at a genuinely heuristic-dead flip with both warnings on — only the unwinnable text showed (`d-05`); (e) F8 amber rings + ghost intact (`b-03`/`b-04`); (f) settings restored to new defaults + Draw 1 + solvable ON, fresh clean deal left on screen, dev mode left ON (pre-session state). 21 solves total, 0 `[stuck-heuristic]` false positives (the gate never had to rescue — heuristic verdict matched the solver every time).
- [x] F12. **Lonelybot optimization feasibility analysis** (analysis only, no implementation): can the solver itself be cheaply made to produce more human/board-first/shorter lines (move-ordering bias, weighted search, post-hoc line compression, optimality search), with effort/risk/benefit vs the F9 app-side approach. DELIVERED — see "Solver-side line-quality feasibility (2026-07-23)" below (verdict: no solver-internal work; F9 suffices).

## Follow-ups

1. ~~Android functional + perf pass~~ — DONE 2026-07-23 as part of F7 (see On-device perf).
2. ~~Commit built artifacts?~~ — RESOLVED 2026-07-23 by F10: not committed; gitignore + `scripts/ensure-rust-solver.js` fingerprint gate + `.easignore`. The universal sim slice stays (the arch-coverage failure mode is silent and nasty — see Identified issues); its 38 MB no longer matters since nothing is committed.
3. **`expo prebuild` regenerates `ios/.xcode.env.local` with a brew-versioned node path** — machine-local booby trap that resurfaces after every clean prebuild once brew upgrades node. Cheap hardening: a `postinstall`/prebuild hook that rewrites it to `/opt/homebrew/bin/node`, or an expo config plugin. Recommendation: leave as-is (documented in the testing skill's troubleshooting table); revisit if it bites a second time.
4. ~~Draw-1 openings produce long draw-hint streaks~~ — ADDRESSED 2026-07-23 by F9 board-first hints (39% of draw-first positions now hint board moves; remaining draw hints are positions where drawing is genuinely the only winnability-preserving action). The "draw N times" copy idea stays unnecessary.

### Solver-side line-quality feasibility (2026-07-23)

F12 deliverable (analysis only, nothing implemented): could the vendored lonelybot search itself produce more human-friendly winning lines (board-first, fewer draw streaks, shorter), instead of / beyond the app-side F9 fix (enumerate no-draw progress moves, verify each with a sub-solve, hint the first that stays winnable)? Facts from `rust/vendor/lonelybot/src/`:

- **Ordering does pick the line, not just the speed**: the solver returns the FIRST line DFS finds (`SolverCallback::on_win` returns `Halt`, solver.rs:33-36) and `traverse` tries moves in exactly the order `MoveMask::iter_moves` yields them (traverse.rs:95).
- **That order is already board-first**: Reveal → PileStack → DeckPile → DeckStack → StackPile (moves.rs:106-120); foundation digs are last; within a type, ascending card index (`Card::from_mask` = trailing_zeros, card.rs:109-117) — no draws-needed awareness.
- **Draws don't exist inside the search**: the 5 primitive move types (moves.rs:10-16) treat the deck as a reachability mask (`Deck::compute_mask`); `convert_moves` expands one deck move into n×DRAW_NEXT+play only afterwards (convert.rs:15-35). "Fewer draws" is not expressible as a search cost without threading deck-offset math through the search.
- **The biggest cause of draw-first lines is ordering-IMMUNE**: dominance forcing returns single-move masks that bypass iteration entirely — forced pile→stack (state.rs:178-184), ≥3-redundant collapse (:190-195), forced deck→stack (:198-206); on draw-1, ANY dominance-safe deck card (e.g. a buried ace) makes that lone deck move the node's only child (`get_deck_mask`, state.rs:136-143). That alone explains the ~18-draw fresh-deal streak in Follow-up #4. These forcings are also why solved positions visit only ~100 states (perf tables) — weakening them for line quality trades orders of magnitude of search perf.
- **Reordering is sound**: pruning is a mask filter computed before iteration (traverse.rs:85-87, `FullPruner::prune_moves`), the TP table is a plain visited-set (traverse.rs:81) — both order-independent. But TP-cut path dependence means a preference order only *tends* to shape which line is found.

| # | Avenue | How, in this code | Quality gain | Perf/soundness risk | Effort | Verdict |
|---|---|---|---|---|---|---|
| 1 | Move-ordering bias | Permute `iter_moves`; sort deck moves by draws-needed (needs state in `MoveMask` iteration — today it's state-free) | Small: already board-first; dominance-forced draws untouched; TP path-dependence dilutes the rest | Sound; per-position solve time can swing either way (budget caps it) | S–M | Skip — F9 fixes the one move the user sees, with exact verification |
| 2 | Post-hoc line rewrite | Replay + greedily hoist later board moves ahead of deck moves, validating each permutation by replay (unpruned legality check exists: `DependencyEngine::is_valid`, dependencies.rs:83-86); the (unused) `DependencyEngine` yields the move partial order in one pass — deck moves form a chain via `last_draw` deps (dependencies.rs:161,187) | Real for FULL-line display; for hints strictly subsumed by F9 applied per request (which can even switch to a different winning line, not just permute one) | Low (replay-verified, fall back to original line) | M | Later — only if a "show winning line"/autoplay feature ships; even then, iterating the F9 preference step along the line is a simpler equivalent (~sub-ms per step) |
| 3 | Weighted/guided search | New search core: standard-move-weighted costs, cost-keyed TP (`TpTable` HashSet→HashMap + node reopening), parent pointers for reconstruction | Whole line genuinely shorter/board-first-er — but invisible per hint once F9 ships | Hard positions visit 12–20M states (perf table) → memory/time cliff; dominance is winnability-safe but NOT optimality-safe, so "optimal" holds only within the pruned space anyway | L | No |
| 4 | ML/"AI" route | `mcts_solver::pick_moves` is a bandit over candidate moves (reversible-closure enumeration, mcts_solver.rs:57-79) scored by HOP rollouts that SHUFFLE the hidden cards (hop_solver.rs:124-127) — an imperfect-information tool that would discard our perfect information and cost 100s of node-limited solves per hint; a learned move-order policy would approximate ~3 deterministic rules with a training pipeline + no_std inference | None over the rule-based approaches | High complexity, nondeterminism, big fixed cost | L+ | No — for a hint feature, "the AI" is the exact solver itself; deterministic preferences win |
| 5 | Other code findings | `graph.rs` enumerates the FULL reachable graph in one traverse (on_win → `Ok`, keeps exploring, graph.rs:64-72) → offline Dijkstra for a "nicest line" per curated/daily deal (unsolvable-proof-class cost: seconds, fine offline, not interactive); `hop_solver::list_moves` = honest-hints engine (never exploits face-down identities) for a possible future "fair hint" setting, ~tens of ms per hint | Different product features, not F9's problem | — | M each | Park as future options |

**Recommendation**: no solver-internal work now — F9 alone. It composes the existing solve as a black box (a few sub-ms sub-solves per hint), fixes exactly the user-visible metric with a winnability proof per candidate, and avoids forking the search internals of the vendored crate (any internal change adds permanent divergence from upstream `ae30d39` plus artifact rebuild + device re-verification overhead). Revisit triggers: (a) "show full winning line"/autoplay feature → avenue 2 (or simpler: iterate F9's preference along the line); (b) curated/daily-deal pipeline wants nicest-line metadata → avenue 5 `graph.rs` offline; (c) an honest-hints setting → avenue 5 `list_moves`. Avenues 1, 3 and 4: not worth it at any trigger we foresee.

### Artifacts-in-git decision (2026-07-23)

**Decision: do NOT commit the built solver artifacts. Commit only Rust source; gitignore all build outputs; add a fingerprint-based ensure step that auto-rebuilds (toolchain present) or fails loudly (toolchain missing) in every build path; add a root `.easignore` so the locally built artifacts still upload to EAS.** Resolves F10 and Follow-up 2. Research done 2026-07-23 (web + repo).

**Research summary** (sources fetched 2026-07-23):

- Canonical git practice: derived binaries don't belong in history; store them in artifact storage or rebuild in CI ([SE: "Do products of a build process belong in a repository?"](https://softwareengineering.stackexchange.com/questions/331774/do-products-of-a-build-process-belong-in-a-repository)). Git packs can't delta binaries — measured here: the 57 MB XCFramework gzips to ~18 MB, so every solver release committed would permanently add ~18 MB to `.git` (which kDrive also syncs — each artifact version stored twice: pack + working copy). The 38 MB sim `.a` is one growth step from GitHub's 50 MB per-file warning. Not catastrophic at "a handful of updates", but permanent and avoidable.
- Rust-in-mobile ecosystem norm: nobody commits XCFrameworks/`.so`s to the repo. Library repos build in CI and attach `*.xcframework.zip` to GitHub Releases, consumed via SPM `binaryTarget` URL+checksum ([Mozilla application-services megazords/ios-rust](https://github.com/mozilla/application-services/tree/main/megazords/ios-rust), [firefox-ios Package.swift](https://github.com/mozilla-mobile/firefox-ios/blob/main/MozillaRustComponents/Package.swift), [Ferrostar](https://stadiamaps.com/blog/ferrostar-building-a-cross-platform-navigation-sdk-in-rust-part-2/)). App repos with vendored Rust build from source on the build machine / EAS workers via `eas-build-pre-install` ([Expo build lifecycle hooks](https://docs.expo.dev/build-reference/npm-hooks); real examples: [vouch-protocol expo-sonic](https://github.com/vouch-protocol/vouch/blob/main/mobile/expo-sonic/README.md), [claas.dev Expo-with-Rust](https://claas.dev/posts/expo-with-rust/)).
- EAS mechanics — the pivotal constraint ([expo/fyi eas-build-archive](https://github.com/expo/fyi/blob/main/eas-build-archive.md), [easignore docs](https://docs.expo.dev/build-reference/easignore)): the upload archive = working-dir files minus `.gitignore` matches, so gitignored artifacts would NOT reach EAS by default and cloud iOS builds would break. But a root `.easignore` REPLACES `.gitignore` for uploads and is the documented way to upload non-committed files. `requireCommit` defaults to false (we don't set it); `eas build:inspect -s archive` verifies the upload. EAS `cache.paths` is a best-effort perf cache only ([caching docs](https://docs.expo.dev/build-reference/caching/)) — never a correctness mechanism.
- git-lfs in 2026: GitHub Free now includes 10 GiB LFS storage + 10 GiB bandwidth/month, $0.07/GiB beyond ([GitHub product-usage docs](https://docs.github.com/en/billing/reference/product-usage-included)) — quota is a non-issue at our scale. Rejected anyway: adds clone/tooling friction and a new silent-failure mode (pointer files where binaries should be, incl. inside EAS archives) for zero benefit once artifacts aren't committed at all.
- Dynamic-library XCFramework to shrink the 57 MB: rejected. The cargo-xcode maintainer is blunt — "use static libraries whenever possible; dynamic libraries are much harder to build, much harder to embed correctly, and add bloat" ([cargo-xcode docs](https://docs.rs/crate/cargo-xcode/latest)); CocoaPods has known mixed static/dynamic vendored-framework failures ([CocoaPods#12172](https://github.com/CocoaPods/CocoaPods/issues/12172)). Once nothing is committed, the fat sim slice costs nothing (never ships; only ~0.5 MB survives app linking).
- Silent-staleness angle (why this beats committing even on the "must always build" axis): a committed artifact goes silently stale when `rust/` changes without a rebuild — git cannot detect that. The fingerprint check detects exactly that and self-heals. Commit-Android-`.so`-only was also rejected: two mechanisms, and rebuilt `.so` bytes differ per toolchain → perpetual dirty diffs.

**Implementation spec (for the follow-up agent):**

1. `.gitignore` — append:
   ```
   # Rust solver build outputs — derived from rust/ by scripts/build-rust-solver.sh.
   # Deliberately NOT committed (see hints doc "Artifacts-in-git decision 2026-07-23").
   modules/soli-solver/android/src/main/jniLibs/
   modules/soli-solver/ios/SoliSolver.xcframework/
   modules/soli-solver/.artifacts-fingerprint.json
   ```
2. Everything else stays tracked source: `rust/**` (`rust/target/` already ignored), `scripts/build-rust-solver.sh`, non-generated `modules/soli-solver/**` (podspec, Swift, Kotlin, `index.ts`, `expo-module.config.json`, `package.json`, `build.gradle`, `AndroidManifest.xml`). Actual `git add`/commits remain the user's call — agents never stage.
3. New `scripts/ensure-rust-solver.js` (Node, stdlib only): fingerprint = sha256 over sorted relative paths + bytes of `rust/` (excluding `target/`) + `scripts/build-rust-solver.sh`. Compare against `modules/soli-solver/.artifacts-fingerprint.json` (`{fingerprint, builtAt, rustcVersion}`) AND verify all four artifacts exist (2 `.so`s, 2 XCFramework slice `.a`s). Match → silent exit 0 (must be <~150 ms). Mismatch or missing → if `rustup` and `cargo` are on PATH: print the reason, run `scripts/build-rust-solver.sh`, re-verify, write the fingerprint file (only after success); else exit 1 loudly with the reason + exact fix (`brew install rustup` + `rustup-init`; the build script self-installs targets and cargo-ndk). Rustc version is recorded for forensics but does NOT gate rebuilds (a plain `rustup update` shouldn't force a 4-target rebuild — trade-off noted here on purpose).
4. Wiring: export `ensureRustSolverArtifacts()` from that script and call it at the start of main() in `scripts/build-install-android.js` and `scripts/build-install-ios.js` (same layer as the existing `android/`-missing prebuild guard in `scripts/lib/build-tools.js`); change package.json `build:ios` to `node scripts/ensure-rust-solver.js && npx eas build --platform ios --profile production --non-interactive`. One script pass builds both platforms (as today; warm rebuilds are seconds) → exactly one fingerprint, no per-platform drift. Jest/typecheck/lint never touch artifacts — unaffected.
5. New root `.easignore` = full copy of `.gitignore` WITHOUT the three artifact lines from step 1, with header comment: "EAS upload manifest — mirrors .gitignore EXCEPT the Rust solver artifacts, which must upload (built locally by ensure-rust-solver; EAS workers have no Rust toolchain). Any .gitignore edit must be mirrored here. `.env` must stay ignored (secrets)." Verify once via `eas build:inspect -p ios -s archive -o /tmp/eas-archive` → XCFramework present in output.
6. Known limitation (also note it in the `.easignore` header): builds NOT started from a machine with artifacts (EAS GitHub integration, `requireCommit: true`) fail loudly at `pod install`/link on the missing vendored framework — never a silently stale solver, because EAS never holds an old copy. If such a workflow ever starts: add an `eas-build-pre-install` hook installing rustup + targets + cargo-ndk and running the build script (~3–5 min/build; soften with `cache.paths: ["~/.cargo", "rust/target"]`).

Rejected alternatives, one line each: commit-all (~18 MB permanent history growth per solver release, kDrive double-sync, against canonical practice); git-lfs (friction + pointer-file failure mode, no need at 10 GiB free); commit Android `.so` only (asymmetric double mechanism, dirty-diff churn); dynamic-framework XCFramework (embedding/codesign risk, ecosystem-discouraged); GitHub Releases + fetch script (library-distribution machinery a solo app repo doesn't need).
