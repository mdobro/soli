
# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli

# keep types in repo to track them
# **/types/**/*.d.ts
# but map can be ignored and just published on npm
**/types/**/*.d.ts.map

.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions

.turbo

.ultra.cache.json
tmp
*.tsbuildinfo
*.tmp.js
yarn-error.log
dist
tsconfig.tsbuildinfo
node_modules
**/_
**/tests/spec/out
.DS_Store
.cursor/debug.log

.next
.vercel
.tamagui
.idea

.env
# local env files
.env.local
.env.development.local
.env.test.localp
.env.production.local

.expo
/ios
/android

# Local Expo modules: keep native sources tracked, ignore build outputs
modules/**/android/build
modules/**/android/.cxx
modules/**/android/.gradle
modules/**/android/.kotlin
modules/**/ios/build
modules/**/ios/Pods
modules/**/ios/.symlinks
modules/**/ios/DerivedData
# Never commit keystore files
*.keystore
*.jks
.venv
scripts/android-unlock-pattern.sh

# Agent test-run artifacts (screenshots, build logs)
.test-artifacts/

rust/target/

# Rust solver build outputs — derived from rust/ by scripts/build-rust-solver.sh.
# Deliberately NOT committed (see hints doc "Artifacts-in-git decision 2026-07-23");
# scripts/ensure-rust-solver.js rebuilds them per fingerprint in every build path.
# NOTE: any other .gitignore edit must be mirrored in .easignore (which replaces
# this file for EAS uploads and must keep these artifact lines OUT).
modules/soli-solver/android/src/main/jniLibs/
modules/soli-solver/ios/SoliSolver.xcframework/
modules/soli-solver/.artifacts-fingerprint.json
